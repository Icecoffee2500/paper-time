name = "paper-time-feedback"
main = "worker.js"
compatibility_date = "2026-09-01"

# Reply-to addresses and the per-address rate counter. Nothing else is stored.
[[kv_namespaces]]
binding = "REPLIES"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
