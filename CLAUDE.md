# Paper Time

Apple 네이티브(macOS/iPadOS/iOS 26) 논문 리더 + 필기 + 서지관리 앱. 전체 계획과 결정 사항은 `PLAN.md`가 정본이다.

## 원칙
- 우선순위: 읽기 경험 > 필기가 PDF 파일에 기록됨 > 서지 정확도 > BibTeX 내보내기.
- UI/UX는 Apple HIG를 따르고 시스템 컴포넌트만 사용한다. 커스텀 위젯·브랜드 색 강요 금지.
- 서지 파싱은 틀린 값을 조용히 저장하지 않는다. 확신 없으면 `needsReview`.
- 사용자 데이터는 조용히 덮어쓰지 않는다. 저장 전 재로드·병합, 충돌은 UI로 노출.
- **사용자의 화면은 건드리지 않는다.** 녹화하지 않고, 화면을 찍지 않고, 키나 클릭을 쏘지 않는다. 확인이 필요하면 **내가 띄운 창을 그 앱이 스스로 찍는다** — 맥은 `--papertime-window-shot=<경로>`, 포터블은 `--papertime-shot=<png>`이고 둘 다 컴포지터에서 제 창만 가져오므로 화면 접근 권한도 포인터 이동도 없다. 합성 이벤트는 앞에 있는 아무 앱에나 들어간다: 실제로 두 번 사용자의 편집기로 들어갔고 한 번은 Cursor의 설정 탭을 열었다. 그래서 프로브는 **페이지 안에서** 이벤트를 만들어 전달한다(`--papertime-probe`). 이 길로 답이 안 나오는 확인은 **사람에게 부탁한다** — 대신 화면을 보려 하지 않는다.
  - **창을 앞으로 띄우는 것도 화면을 건드리는 것이다.** `open -n -a`는 앱을 **활성화한다** — 창이 사용자가 하던 일 위로 올라오고 키보드를 가져간다. 한 세션에 열 번 넘게 그랬다. 그래서 프로브를 띄우는 길은 **`Scripts/probe.sh` 하나뿐이다**: `open -g -j`(활성화하지 않음 + 숨긴 채 시작)로 띄우고, `--papertime-library=`가 없으면 **실행을 거부하고**(프로브는 진짜 라이브러리를 열면 안 된다 — 설정 도메인이 설치본과 같다), 끝나면 이름이 아니라 **라이브러리 경로로** 죽이고(`/Applications`의 사본은 건드리지 않는다), 앞에 있던 앱이 바뀌었으면 크게 알린다. 인자 없이 번들 속 실행 파일을 직접 돌리면 stdout이 어디로도 안 가니 그 길은 없다. 숨긴 앱에는 보이는 창이 없어서 `--papertime-window-shot`이 «no window»를 돌려줬다 — 그래서 프로브가 제 창을 **모든 디스플레이 밖으로** 옮겨 놓고 거기서 그린다(`WindowProbe.offscreen`, 화면들의 합집합 기준으로 잡는다 — 왼쪽에 모니터가 하나 더 있으면 음수 좌표에 진짜 픽셀이 있다). 사진은 오히려 더 잘 나온다: 예전에 비어 보이던 사이드바가 찍힌다. `--papertime-window-shot-quit=1`은 시트가 떠 있으면 안 먹으니 종료는 스크립트가 맡는다.
  - **포터블도 같은 문으로만 띄운다: `Scripts/probe.sh -p`.** 포터블 창은 `ready-to-show`에서 `show()`를 불렀는데, 맥에서 `BrowserWindow.show()`는 **앱을 활성화한다** — `open -n -a`와 똑같이 사용자가 하던 일 위로 올라온다. 맥 쪽에는 `probe.sh`가 있었지만 포터블에는 아무 문도 없었다. 지금은 프로브 실행(`--papertime-library`·`--papertime-probe`·`--papertime-shot` 중 하나라도 있으면, `isProbeRun()`)이면 창을 **모든 디스플레이의 합집합 바깥**에 놓고 `showInactive()`로 띄우며, 맥에서는 `setActivationPolicy('accessory')` + `dock.hide()`로 독에도 안 나타난다. 창 크기·자리·최대화도 설정에 쓰지 않는다(바깥에 놓은 자리가 사용자의 창 자리가 된다). `probe.sh -p`는 `Portable/out`을 `open -g -j`로 띄우고 같은 규칙(라이브러리 없으면 거절, 라이브러리 경로로 종료, 앞 앱 감시)을 지킨다 — 먼저 `Portable/`에서 `npm run build`. 확인: 페이지에서 `screenX`가 화면 너비보다 크고 `document.hasFocus()`가 false다.
  - **맥 프로브의 라이브러리는 앱 컨테이너 안에 둔다** — `~/Library/Containers/com.imtaeheon.PaperTime/Data/tmp/<이름>`. 샌드박스 앱은 `/private/tmp/…`의 폴더를 못 읽는데, 못 읽는다고 말하지도 않는다: stderr에는 «opening the library»까지만 찍히고 그 뒤로 **아무것도** 안 나온다(`--papertime-folders`도, 스케치 프로브도). 포터블은 샌드박스가 없어서 어디든 된다.
  - **숨긴 채 띄운 앱은 창을 스스로 안 열 수도 있다.** `open -g -j`로 띄운 맥 프로브가 «app starting»만 찍고 멈춘 적이 있다 — 메인 스레드는 런 루프에서 놀고 있었고, 로그에는 `hasPersistentStateToRestore=1`인데 되살린 창이 없었다. 프로브가 창을 얻던 길은 AppKit이 **지난 세션의 창**을 되살려 주는 것뿐이었는데, 샌드박스가 하나라 그 지난 세션은 사용자 사본의 것이었다. 창이 없으면 `RootView`의 task가 안 돌고, 라이브러리도 프로브 훅도 안 돈다. 그래서 `probe.sh`가 `-ApplePersistenceIgnoreState YES`(인자라 아무것도 저장하지 않는다)를 주고, 1.5초 안에 아무도 실행을 시작하지 않으면 앱이 제 창을 하나 만든다(`WindowProbe.openIfNoneWasRestored`, `--papertime-library`가 있을 때만). 실행은 창이 몇 개 떠도 한 번만 시작한다(`AppModel.beginLaunch`). **제목 막대가 있는 창은 AppKit이 화면 안으로 끌어온다** — −60000에 놓고 띄우자 (218, 0)에 떴다. 화면 밖에 둘 창은 `constrainFrameRect(_:to:)`를 그대로 돌려주는 하위 클래스로 만들고, 띄우기 전과 뒤에 화면과 겹치는지 본다.
  - **포터블 프로브는 사용자의 설정을 읽지만 쓰지 않는다.** 쪽 배치·색조·언어가 그 사람 것이라, 한 쪽 보기로 되어 있으면 다른 쪽이 `display: none`이다 — 쪽을 건너는 끌기는 먼저 `{"menu": "layoutContinuous"}`를 보낸다. 프로브 실행에서 `update()`는 메모리에만 남긴다(`holdInMemory`) — 전에는 논문 하나만 열어도 사용자의 `settings.json`에 `selectedPaperID`·`pageLayout`·`inspectorTab`이 써졌다. 워크트리의 `Portable/node_modules`는 본 체크아웃으로 가는 링크라서 `probe.sh`는 앞 앱을 링크를 푼 경로(`pwd -P`)로도 비교한다.
  - **끌기를 프로브로 할 때.** 채우기 없는 도형은 **테두리에서만** 잡힌다 — 한가운데를 누르면 선택이 풀리고 끌기가 올가미가 된다. 선택된 원소는 손잡이(모서리·변의 가운데)도 피한다: 왼쪽 변 가운데를 잡았다가 옮기기가 크기 바꾸기가 되어 폭이 117에서 409가 됐다. 밑변의 4분의 1 지점이 안전하다. 수식키는 **누른 뒤에** 건다 — ⇧를 누른 채 누르면 «선택에 더하기/빼기»라서 옮길 것이 선택에서 빠진다(맥 `shiftdrag`·`cmddrag`, 포터블 `drag`의 `keys`는 움직임과 떼기에만 수식키를 싣는다). 떠 있는 동안만 그려지는 것(정렬선, 남겨 둔 사본)은 맥 `…draghold`, 포터블 `hold: true`로 버튼을 누른 채 찍는다.
  - **명령줄 맥 빌드는 ad-hoc 서명을 준다**: `CODE_SIGN_IDENTITY="-" CODE_SIGN_STYLE=Manual PROVISIONING_PROFILE_SPECIFIER=""`. 새 워크트리에서는 먼저 `xcodegen generate`(프로젝트는 생성물이라 git에 없다) — 없으면 «requires a development team»으로 멈춘다.

## 기술 제약
- 무료 Apple 개발자 계정: CloudKit/iCloud 컨테이너 entitlement 사용 불가. 라이브러리는 사용자가 고른 클라우드 폴더(iCloud Drive/Google Drive)에 파일로 저장한다(`PLAN.md` 3.1).
- LLM은 온디바이스 Foundation Models만 사용. 클라우드 LLM 코드 추가 금지.
- Overleaf 자동 연동 없음. `.bib` 내보내기 품질에 집중.

## 코드 구성
- Swift 6, SwiftUI, Swift Package로 모듈 분리: PaperCore, LibraryStore, PDFReader, InkEngine, MetadataPipeline, Bibliography, Importers.
- 테스트 코퍼스 PDF는 저장소 밖 `~/Documents/Bookends/Attachments`(58편) + `~/Documents/Bookends/vla`(3편). 정답은 `Tests/Corpus/ground_truth.json`에 둔다. PDF를 저장소에 커밋하지 않는다.

## 빌드/실행
- **맥 최소 버전은 14(Sonoma), 아이패드·아이폰은 26.** `project.yml`의 `deploymentTarget.macOS`와 `Package.swift`의 `.macOS("14.0")` 둘이 한 쌍이다. 26/15 전용 API는 `#available`로 가른다 — 지금 갈라 둔 것: FoundationModels 전체(`OnDeviceHeaderExtractor`, 26), 도구 막대의 `ToolbarSpacer`·`sharedBackgroundVisibility`(26)와 `toolbarBackgroundVisibility`(15) → `RootView.decorated`의 두 가지, `onScrollGeometryChange`(15) → `View.onPull`, `Color.mix`(15) → `Color.mixed`. FoundationModels는 `OTHER_LDFLAGS`로 **약하게 링크**한다 — 강하게 걸면 Sonoma의 dyld가 앱 자체를 안 띄운다(`otool -l … | grep -B3 FoundationModels`로 `LC_LOAD_WEAK_DYLIB`인지 본다). `CustomizableToolbarContentBuilder`는 없다 — 도구 막대 내용은 `#available`로 감싼 헬퍼를 못 만들고 호출 자리에서 가른다. 이 컴퍼일이 통과한다는 것과 Sonoma에서 돈다는 것은 다르다: 실기 확인은 Sonoma VM에서 한다(이 맥에는 VMware Fusion 13이 있고, `softwareupdate --list-full-installers`에 14.8.x 설치 프로그램이 있다).
- Xcode 26.6. 앱 타겟 실행은 Xcode 또는 `xcodebuild`. 시뮬레이터 확인은 iOS Simulator 도구 사용.
- iOS 빌드: `xcodebuild -scheme PaperTime -destination 'platform=iOS Simulator,id=<UDID>' -derivedDataPath build/ios CODE_SIGNING_ALLOWED=NO build`, 설치·실행은 `xcrun simctl install/launch`. 시뮬레이터를 손대지 않고 몰고 가려면 환경변수: `SIMCTL_CHILD_PAPERTIME_LIBRARY=PaperTimePapers`(앱 Documents 안 상대경로), `…_SKIP_WELCOME=1`, `…_ADOPT_LOOSE=1`(폴더의 PDF를 들여옴), `…_OPEN_FIRST=1`(첫 논문을 연다). 시뮬레이터에서 필기·지우개를 시험하려면 AA 메뉴의 Draw with Finger를 켠다; 탭을 여러 개 이어 보내면 메뉴 애니메이션에 먹히니 한 탭마다 스크린샷으로 확인한다. 캡처는 `xcrun simctl io <UDID> screenshot`.
- 필기 층(세 기기 공통): 하이라이트·밑줄은 PDF 주석이고 `MarkOverlayView`가 둥근 끝으로 그린다. 잉크는 쪽마다 사이드카 `.papertime/papers/<id>/ink/pNNNN.drawing`이 **진실**이고, PDF 속 잉크 주석은 저장 때 써 내는 사본이다(경로는 주석 bounds 기준 좌표 — PDFKit이 `/InkList`에 원점을 더한다). 파일에만 있는 잉크는 열 때 `InkConverter.drawing(fromOwnedInkOn:)`으로 사이드카를 만든다. 오버레이 아래에서는 PDF의 잉크 사본을 항상 숨긴다(`hideOwnedInk`). 아이패드·아이폰은 `PKCanvasView`가, 맥은 `InkOverlayView`(PKDrawing 렌더)가 사이드카를 그린다. 지우개는 `PageOverlay`가 직접 받는다(캔버스 위의 팬 제스처는 스트로크가 시작되면 취소된다).
- 펜 도구: `ReaderConfiguration.tool/presets`(`InkTool`, `PenColor`, `InkPresets`)가 상태, `MarkingToolbar`(iOS)가 제목 아래 도구 줄. PKToolPicker는 쓰지 않는다 — 캔버스 tool은 `configuration.currentTool`로 코디네이터가 맞춘다. 형광펜만 글자에 맞춘다(위=하이라이트, 아래=밑줄); 펜은 절대 스냅하지 않는다.
- **PDF는 다시 쓰지 않고 뒤에 덧붙인다(`PDFUpdate`, 1.0.0부터).** PDFKit이 문서를 써 내는 모든 길(`dataRepresentation()`, `write(to:)`)이 글꼴을 다시 서브셋해서 TeX 논문의 «ff»를 «!»로 만들었다 — 파일 안에서, 모든 리더에게. 그래서 표시는 증분 업데이트(ISO 32000 §7.5.6)로 원본 바이트 뒤에 붙고(`IncrementalWriter`, 새 주석만 빈 쪽의 스크래치 문서로 직렬화해 옮겨 심는다), 의심스러우면 아무것도 안 쓰고 `keptInApp`으로 남긴다. `Tests/PDFReaderTests/IncrementalSavingTests`의 소스 검사가 `Sources/PDFReader`·`PDFUpdate`·`App`에서 그 두 호출을 금지한다. 코퍼스 확인은 `swift run -c release papertime-pdfcheck protocol|growth|text`.
  - **파일이 어디 서 있는지는 지문이 말한다(`FileProvenance`).** 기록의 `importDigest`(들여올 때 SHA-256, 절대 안 고친다)와 `byteSize` 둘로 정확히 갈린다: 전체가 같으면 `pristine`, 앞 `byteSize` 바이트가 같으면 `appended`, 둘 다 아니면 **다른 앱이 다시 쓴 것**(`rewritten`) — 글꼴·Producer로는 못 가른다(원본 63편 중 38편이 이미 Quartz Producer다). 해시는 한 번 훑으며 접두사에서 해셔를 복사해 둘을 같이 낸다. 앱은 `LibraryModel.provenance(of:)`가 처음 물을 때 배경에서 계산하고 (크기, 수정일)로 캐시한다; 인스펙터 «파일» 절의 조용한 한 줄이고 `rewritten`일 때만 단추가 선다. 창 없이: `--papertime-provenance=1`.
  - **기준선은 사이드카에 둔다: `.papertime/papers/<id>/pdf/base.json`(`PDFBase`, 길이+SHA-256).** 첫 덧붙임 직전의 파일이 기준선이다(`DocumentSession.write(recordingBaseIn:)`가 한 번만 쓴다) — 원본이면 기록과 같은 값이고, P1 전에 PDFKit이 다시 쓴 파일이면 그 파일이 기준선이다. `meta.json`에 키를 더하지 않은 이유: 레코드 바이트는 Portable과 바이트까지 같아야 하고, 모르는 키 하나가 그쪽이 다시 쓰는 레코드마다 클라우드 충돌이 된다.
  - **접기(compaction)는 리베이스다(`Compaction.run`).** 리더를 닫거나 앱이 배경으로 갈 때(`DocumentSession.close`), 꼬리가 max(1 MiB, 기준선/10)을 넘거나 리비전이 100을 넘을 때만 — 또는 댓글을 고치거나 지웠을 때(옛 글이 히스토리에 남아 읽히므로 `historyHoldsOldWords`가 크기와 무관하게 접는다). 기준선 바이트 위에 **보통의 저장기를 전체 상태로 한 번** 돌려 후보를 만들고, 다음이 전부 맞을 때만 바꾼다: 기준선 뒤 리비전이 주석 말고 아무것도 안 바꿨고(`RevisionAudit` — 카탈로그·Info·쪽 사전(/Annots 빼고)·주석이 닿지 않는 객체), 후보가 쪽마다 **같은 표시**를 보이고(`AnnotationComparison.markDifference`, 팝업 제외 — 다른 앱이 접힐 리비전에 넣은 주석은 여기서 걸려 **거절**된다, 옮기지 않는다), 꼬리가 반 이하로 줄고, 파일이 읽었을 때 그대로다(`FileOperations.replace(ifStill:)`). PDFKit의 전체 재작성은 절대 아니다. 창 없이: `--papertime-compact=1`. **«같은 표시»는 사전이 같다는 뜻이 아니라 읽는 쪽이 같은 것을 본다는 뜻이다(`AnnotationMeaning`).** 처음에는 `Canonical.form`(키 전부)으로 비교했고, 그래서 포터블이 쓴 표시가 든 파일은 전부 거절됐다 — 같은 표시를 PDFKit은 다르게 적는다: 두 줄짜리 형광펜이 저쪽은 주석 하나(`/QuadPoints` 여덟 개 둘)이고 이쪽은 줄마다 하나, 잉크는 PencilKit을 지나며 1.5pt 간격으로 다시 샘플링되고(41점이 161점), `/Border`·`/DA`·`/T`·`/BS`의 모양·외형 스트림·`/PTInkStroke`는 쓰는 쪽의 것이다. 그래서 우리 표시는 **뜻으로** 줄인다: 형광펜·밑줄은 종류·`/PTMarkupID`·모든 사각형(0.1pt, id로 합쳐서)·색(1/255)·`/PTComment`(둘 다 없으면 `/Contents`는 안 본다 — 그건 각자 쪽에서 딴 인용문이다), 잉크는 색·굵기(0.1pt)·경로(서로 0.5pt 안이면 같은 선 — 실측 0.13pt), 도형은 `/PTSketchID`·부분·주석 종류·`/PTSketch`를 JSON으로 푼 것. 남의 주석은 여전히 키 전부다. 접을 때의 상태에는 **우리가 쓴 표시만** 넣는다(`TextMarkupWriter.ownIdentifiers` + 저널) — `markups`에는 자리로 이름 붙인 남의 형광펜도 들어 있고, 그걸 다시 쓰면 우리 다섯 색 중 가장 가까운 색을 입은 우리 것이 된다. 그리고 `/PTComment` 없는 표시의 `/Contents`는 PDFKit이 상자에서 읽은 인용문과 **띄어쓰기·대소문자·문장부호를 빼고** 견준다(`isQuotation`) — 엄격히 비교하니 포터블의 밑줄마다 제 인용문이 댓글로 붙어 왔다. 픽스처는 포터블의 진짜 저장기로 만든다(`Portable/tools/cross-fixture.mjs` → `Tests/PDFUpdateTests/Fixtures/portable-{base,appended,history}.pdf`, 101회 저장본 180 KB가 24.9 KB로 접힌다), 접힌 결과는 포터블이 다시 읽는다(`src/test/fixtures/mac-compacted.pdf`).
  - **«원본 글자 되살리기…»(`PaperRestore`)는 사람이 고른 파일로만, 지문이 정확히 같을 때만.** 다시 쓰인 파일은 고칠 길이 없지만 들여온 원본은 대개 다운로드 폴더에 남아 있다. 고른 파일의 SHA-256이 `importDigest`와 같아야 하고(비슷한 것은 안 받는다), 쪽 수가 같아야 하며, 알림이 먼저 무엇이 바뀌는지 말한다(PDFKit이 읽는 글자가 다른 쪽 수 — LeJEPA는 23쪽). 그 다음 저널+사이드카+**파일에만 있던 우리 표시**+**다른 앱의 주석**(원본에 없는 것을 PDFAnnotation 객체째 옮긴다)을 증분 저장기로 원본에 쓰고, PDFKit이 읽는 표시가 지금 파일과 같은지 본 뒤(`PaperRestore.difference` — `/PTComment`는 안 본다, P1 전 파일에는 없다), 지금 파일을 **휴지통**으로 보내고(`FileManager.trashItem`, 안 되면 라이브러리 `Trash/`) 새 파일을 놓는다. 열린 세션은 `fileWasReplaced()`로 문서를 바꿔 낀다(`updateNSView`가 `document`의 identity가 바뀌면 다시 넣는다). 네트워크 없음. 창 없이: `--papertime-restore-from=<컨테이너 안 경로>`; 패널은 프로세스 밖이라 사람 손으로만 확인된다.
- 동기화(iCloud Drive만; 근거리 전송은 뺐다): 표시는 기기별 저널 `.papertime/papers/<id>/marks/<device>.json`에 즉시, PDF에는 1.5초 뒤 저널대로 다시 쓴다(`DocumentSession.reconcile`). `DocumentWatcher`(파일 프리젠터+kqueue)와 3초 폴링(`requestPendingDownloads`)이 도착을 알린다. CloudKit(푸시)은 무료 계정으로 쓸 수 없다.
- **일이 끝나면 `build/`의 앱을 지운다.** 디버그로 확인하고 나면 `build/mac/Build/Products/Debug/Paper Time.app`이 남는데, 그건 **아무도 열면 안 되는 사본**이다(아래 항목: Debug 빌드는 시작이 몇 초 느리다). 폴더에 있는 사본은 언젠가 누가 두 번 누른다. 그래서 세션이 끝날 때 `Scripts/tidy.sh`를 돌려 앱 번들만 지운다 — 인덱스와 오브젝트 파일은 남겨야 다음 빌드가 빠르다. `Scripts/publish-release.sh`도 배포 끝에 같은 일을 하지만, 배포로 끝나지 않는 날이 더 많다. `Scripts/tidy.sh --all`이면 `Installers/`·`dist/`·`Portable/dist`의 패키지까지 비운다.
- **쓰라고 설치하는 것은 Release다.** `~/Applications`에 Debug 빌드를 넣어 두고 있었는데, Swift의 Debug 빌드는 문자열·컬렉션이 많은 코드에서 몇 배 느리다 — 실측으로 시작할 때 메인 스레드가 멈추는 시간이 4.8초(Debug) 대 2.8초(Release)였다. 디버깅은 `build/mac`(Debug)로 하고, 사용자가 여는 사본은 `xcodebuild -configuration Release -derivedDataPath build/release CODE_SIGN_IDENTITY="-"`로 지어 넣는다.
- 느려졌다 싶으면 재지 말고 **자 대고 재라**: `--papertime-trace=1`(또는 `PAPERTIME_TRACE=1`)이면 4 ms를 넘는 단계와 메인 스레드가 50 ms 넘게 멈춘 자리를 찍고, 앱이 끝날 때 무엇에 시간이 갔는지 합계를 낸다(`Trace`, `Hitches`). 곁들여 `sample "Paper Time" 5 -file …`으로 그 순간의 스택을 보면 어디인지 나온다 — 이 방법으로 찾은 것이 "행마다 컨텍스트 메뉴를 통째로 짓고 있었다"였다.
- 실행 설정은 환경변수와 **명령줄 인자 둘 다**로 준다(`Boot`): `PAPERTIME_OPEN_TITLE=EWC`와 `--papertime-open-title=EWC`가 같다. 번들을 `open`으로 띄울 때는 환경변수를 넘길 길이 없어서 인자 쪽이 필요하다 — `open -a "…/Paper Time.app" --stderr /tmp/t.log --args --papertime-trace=1`.
- **표 분석은 꺼 두었다.** `-[PDFView setDocumentAnalysisEnabled:]`(헤더에 없는 PDFKit의 스위치, `responds(to:)`로 확인해서 KVC로 내린다)가 macOS 26의 Vision 표 분석과 셀 선택 모드를 함께 끈다 — 사용자가 표 안에서 자유롭게 드래그하고 싶어 했고, 아래 두 항목의 병리(쪽을 배경에서 다시 쓰는 것, 줄 자리가 없는 선택)도 그 분석이 근원이다. 켜져 있던 시절의 기록은 그대로 남겨 둔다. 스위치를 찾은 방법: `objc` 런타임으로 PDFKit 클래스의 메서드 이름을 훑었다(`class_copyMethodList`; 전체 클래스 목록을 돌면 이상한 클래스에 걸려 죽으니 이름을 지정해서). `dyld_info -objc`는 공유 캐시 안의 프레임워크에는 안 먹는다.
- **표가 있는 쪽은 PDFKit이 제 스레드에서 만지고 있다.** macOS 26의 PDFKit은 새로 보이는 쪽을 Vision에 넘겨 표를 찾고(`PDFPageAnalyzerV2 addTablesFromVisionDocument:…toPage:`, `PDFKit.PDFDocument.formFillingQueue`), 그 결과를 쪽에 되쓴다 — 같은 사각형을 두 번 선택하면 줄 수가 31개에서 76개로 바뀌는 것으로 확인된다. 그러니 **쪽을 오래 붙들지 마라**: 표시를 글자에 맞추려고 줄마다 `page.draw`를 부르던 코드가 표 하나에 백 번을 그렸고, 그게 표에서만 앱이 죽던 자리다(`InkStrip`은 이제 표시 하나에 한 번만 그린다). 끄는 API는 없다.
- **Swift 태스크 안에서 Objective-C 예외가 나면 그 자리에서 안 죽고 다음 아무 곳에서 죽는다.** AppKit이 예외를 삼키는 동안 스택이 `swift_job_run`을 뚫고 풀려서 동시성 런타임의 스레드 로컬이 죽은 스택 프레임을 가리키게 되고, 그 다음 `@MainActor` 메서드에 들어갈 때(`swift_task_isCurrentExecutor`)에 `swift_getObjectType`에서 죽는다 — 크래시 로그에는 `mouseMoved`, `viewDidMoveToWindow` 같은 무관한 곳이 찍히고, 옆에 `HIE:` 스레드(`SOME_OTHER_THREAD_SWALLOWED_AT_LEAST_ONE_EXCEPTION`)가 서 있다. 그 로그가 보이면 크래시 자리를 보지 말고 **`/usr/bin/log show --predicate 'processImagePath CONTAINS "Paper Time" AND messageType == error'`로 몇 초 전의 `*** Assertion failure`를 찾아라**(zsh의 `log`는 내장 명령이라 `/usr/bin/log`). 실제 사례: 표를 가로지른 선택에 PDFKit이 `bounds(for:)`를 `nan`으로 돌려줬고(`isEmpty`도 `isNull`도 `nan`을 통과시킨다 — `CGRect.isFinite`가 그래서 있다), 표시 막대를 `{nan, nan}`에 세우려다 `NSWindow.m:1070`이 던졌다. AppKit에 넘기는 좌표는 반드시 유한한지 본다.
- 배경 스레드에서 `page.addAnnotation`을 부르면 PDFKit이 `PDFViewAnnotationsDidChange`를 **그 스레드에서** 던지고, 화면의 PDFView가 그걸 받는다. 저장할 때 아는 표시를 전부 다시 넣던 시절 한 번 저장에 2만 3천 개가 갔다(`TextMarkupWriter.isAlreadyWritten`으로 멈췄다). 알림이 어느 스레드로 오는지는 `--papertime-watch-threads=1`이 메인 스레드 아닌 곳에서 온 알림을 모두 찍는다.
- 표시가 얼마나 걸리는지는 마우스 없이 잰다: `--papertime-mark-test=<쪽>,<x>,<y>,<w>,<h>`가 그 사각형을 형광펜으로 칠하고, 줄 수·사각형 수·걸린 시간을 찍고, 도로 지운다(남의 라이브러리에 자국을 남기지 않는다). 드래그는 스크립트로 못 만든다 — PDFKit의 `shouldBeginDrag:`가 제 이벤트 루프를 돌려서 합성한 이벤트로는 빠져나오지 못한다.
- 표 셀을 마우스로 잡은 선택은 **전체 `bounds(for:)`는 정상인데 `selectionsByLine()`의 줄은 `(inf, inf, 0, 0)`(null)**이다. 이 값은 PDFKit이 표를 분석한 쪽에서 마우스로 만든 선택에만 나오고, 같은 글자 범위를 `page.selection(for: NSRange)`로 만들면 멀쩡하다 — 그래서 스크립트로 재현이 안 되고 사람 손이 필요하다. `TextMarkupWriter.placement(of:on:within:)`가 줄 → 글자 범위로 재선택 → 선택 전체 순으로 묻는다. 선택이 무엇을 들고 오는지는 `--papertime-watch-selection=1`이 바뀔 때마다 stderr에 찍는다(전체 bounds, 줄별 bounds, 글자 범위, characterBounds — 마지막 것은 이 문서들에서 엉뚱한 값이라 쓰지 않는다). `--papertime-mark-test-zoom=2.5`로 확대해 놓고 `--papertime-mark-test-dry=1`이면 가서 자리만 찍고 아무것도 쓰지 않는다 — 표시를 쓰면 파일이 바뀌고 감시자가 다시 읽어 스크롤이 튀어서, 방금 찍은 자리가 틀려진다.
- **창을 찍은 그림을 믿지 마라 — 뷰 나무를 봐라.** `cacheDisplay`는 `draw(_:)`를 따라 걷기 때문에 Core Animation으로 그려지는 것을 통째로 놓친다. 그래서 글자로 가득한 인스펙터가 **까만 사각형**으로, ScrollView에 든 첫 화면이 **텅 빈 회색**으로 찍힌다 — 앱의 버그가 아니라 카메라의 버그다(`layer.render(in:)`도, 접근성 트리를 KVC로 훑는 것도 SwiftUI에서는 마찬가지로 빈손이다). 화면에 무엇이 있는지 확인해야 하면 `--papertime-window-shot=<경로>`에 **`--papertime-window-dump=1`**을 같이 준다: 창의 뷰 나무를 찍어 주고, 거기 `HostingScrollView → NSClipView → DocumentView → _FocusRingView(98×28)`가 보이면 단추는 실제로 거기 있는 것이다. `--papertime-force-setup=1`은 라이브러리를 잊지 않고 첫 화면만 보여주고, `--papertime-choose-folder=1`은 폴더 고르기 창이 정말 뜨는지 열어 보고 닫는다.
- 인용이 논문의 구조를 제대로 읽어 오는지는 창 없이 본다: `swiftc -O App/Model/MathReader.swift App/Model/MathTranscriber.swift App/Model/PDFContentScanner.swift App/Model/TeXGlyphNames.swift Scripts/quote-probe.swift -o /tmp/quote` 뒤에 `/tmp/quote <pdf> <쪽> [x y w h]`면 ⌘L이 노트에 넣을 마크다운이 그대로 찍히고, `--pieces`를 붙이면 그 전 단계(무엇을 제목·수식·산문으로 읽었는지, 본문 대비 몇 배 크기인지)가 나온다. 선택은 앱 안 세 칸 깊이에 있어서 스크립트로 만들 수 없다.
- 창을 띄우지 않고 확인하기: 노트 조판은 `PAPERTIME_DUMP_NOTE=<마크다운 그 자체>`가 런 단위로 찍는다(샌드박스라 경로는 못 읽는다). `PAPERTIME_DUMP_NOTE_IMAGE=<컨테이너 안 경로>`를 같이 주면 같은 노트를 그려 PNG로 남긴다 — 인용의 세로줄과 조판된 수식은 그려지는 것이라 속성 목록에는 나오지 않으니, 그 둘은 그림으로만 확인된다. 본문 검색 색인은 앱 없이도 돈다: `swiftc -O App/Model/PaperTextIndex.swift Scripts/search-probe.swift -o /tmp/probe && /tmp/probe unlearning ~/Documents/Bookends/Attachments`(`PaperTextIndex`가 Foundation·PDFKit 말고는 아무것도 안 쓰는 이유). 팔레트 자체는 `PAPERTIME_SHOW_SEARCH=<찾을 말>`로 그 말이 적힌 채 열리고, `PAPERTIME_SEARCH_JUMP=1`을 더하면 첫 본문 결과를 눌러 그 줄로 간다 — 키를 쏘지 않고 끝까지 보는 길.
- **그리기 층(맥의 필기).** 맥에는 PKCanvasView가 없고 PDFKit의 쪽 오버레이는 마우스를 못 받는다(클릭은 안쪽 문서 뷰로 간다). 그래서 펜을 들면(`ReaderConfiguration.mode == .draw`, ⇧⌘D) `SketchInputView`가 PDFView 위에 통째로 얹혀 마우스를 받고, 스크롤·핀치는 아래 **스크롤 뷰**로 넘긴다 — PDFView가 아니다. PDFKit은 `magnify(with:)`를 구현하지 않고 NSScrollView의 확대로 줌하므로, PDFView에 넘긴 핀치는 응답 체인을 타고 올라가 아무 일도 하지 않았다(0.7.2까지 펜을 든 채로는 핀치 줌이 안 되던 이유). 도구는 Figma의 도구 줄 모양이다: `SketchTool`(V 선택, F 프레임, 도형 묶음 R 네모·O 동그라미·L 선·A 화살표, 펜 묶음 P 펜·H 형광펜·E 지우개, T 글; 묶음은 마지막에 쓴 도구를 단추로 보이고 옆 화살표가 목록을 편다). 상태는 `ReaderConfiguration.sketch: SketchState`, 도구 줄은 `SketchToolbar.swift`, 오른쪽 인스펙터는 `SketchInspector.swift`(위치·정렬·X/Y, 레이아웃·W/H·흐름·간격·여백, 외형·투명도·모서리, 채우기·외곽선(색상 패널·hex·%), 글·크기·정렬·자동 너비/높이). 펜은 마우스 점을 `PKStroke`로 만들어 잉크 사이드카에 넣는다(태블릿 펜의 압력은 굵기에 반영) — 그래서 아이패드와 같은 선이다. 도형·화살표·글 카드·프레임·묶음은 **`SketchElement`**(InkEngine, 쪽 좌표)이고 사이드카 `.papertime/papers/<id>/sketch/pNNNN.json`이 진실, PDF에는 저장 때 표준 주석 사본을 쓴다(네모·프레임→Square, 동그라미→Circle, 곧은 선→Line(끝 모양은 /LE), 굽은 화살표·막대 끝→Ink, 글→FreeText, 상자 속 글→FreeText 하나 더, 묶음→테두리 0·숨김 Square). 사본마다 `/PTSketch`에 원소의 JSON(base64)을 실어 두어 사이드카 없는 기기가 파일에서 그대로 되살린다(`SketchWriter.elements(fromOwnedOn:)`). 사본의 `userName`은 "Paper Time Sketch" — "Paper Time"이면 `InkConverter.isOwned`가 굽은 화살표를 펜 선으로 들여온다. PDFKit으로는 `/CA`(투명도)를 못 쓴다(`setValue(forAnnotationKey:)`가 거절) — 채움은 흰 종이 위에 미리 섞은 불투명색으로 쓴다. 그리기는 `SketchRenderer`(CoreGraphics+CoreText, 쪽 좌표) 하나를 맥 `SketchOverlayView`(PageImages.swift)와 iOS `SketchOverlayView`(PageOverlay_iOS.swift, 뒤집힌 컨텍스트라 `flipsText`)가 같이 쓴다. 되돌리기는 `SketchUndo`가 세션을 타겟으로 쪽 단위 전/후 배열(과 잉크 전/후)을 등록한다. 창 없이 확인: `--papertime-draw=1 --papertime-sketch-sample=1 --papertime-sketch-shot=<컨테이너 안 png>`(테스트 라이브러리 `--papertime-library=TestLibrary`에서만 동작)가 표본 도형을 놓고 창을 PNG로 남기며, stderr에 쪽이 화면 어디에 있는지(Quartz 좌표)를 찍어 실제 포인터 드래그를 보낼 수 있다. `--papertime-sketch-script="tool=frame;drag=…;key=cmd-g;dragpage=1,…;report=1"`이 제스처를 순서대로 돌린다(`dragpage`는 다른 쪽에 놓는 드래그, `report=<쪽>`은 그 쪽의 원소를 부모·레이아웃과 함께 찍는다).
- **그리기의 나무(프레임·묶음·오토 레이아웃).** 배열은 평평하게 두고(파일이 평평한 목록이고, 렌더러와 PDF 사본이 다 그렇게 걷는다) `parent` 필드로 나무를 읽는다(`SketchTree`). **자식의 좌표도 쪽 좌표다** — 부모 상대가 아니다 — 그래서 옛 렌더러와 PDF 사본은 아무것도 몰라도 된다. 프레임(`.frame`)은 제 상자가 있는 컨테이너(채움·테두리·모서리, `clips`면 넘친 자식을 자르고, `layout`이 있으면 자식을 줄·열로 놓는다), 묶음(`.group`)은 제 상자가 없는 컨테이너(상자는 자식들의 합이고, `points`에 그 합을 캐시해 둔다 — 묶음을 모르는 쪽도 온전한 상자를 보도록). 클릭 규칙은 Figma의 것: 묶음은 통째로 잡히고(가장 바깥 묶음), 프레임은 클릭을 자식에게 흘린다; 더블클릭은 묶음 안으로 들어간다(`entered`). 모든 변경은 `SketchTree.normalized`를 지난다 — 부모 뒤에 자식이 오도록 배열을 재배열하고, 안쪽 프레임부터 레이아웃을 적용하고(`arrange`, hugs면 프레임이 자식에 맞춰 크기를 잡되 왼쪽 위 모서리는 고정), 묶음의 상자를 다시 잡는다. 놓은 자리의 프레임에 자동으로 들어가고(`adopted`: 가운데가 들어가는 가장 위 프레임), 묶음에서는 끌어내지 않는다. 다른 쪽으로 끌어 옮기기는 `finishMove`가 두 쪽의 사이드카를 한 undo 그룹으로 고친다(원소는 id를 유지하고 좌표는 목표 쪽의 것). 글 카드는 `textSizing`이 `autoWidth`(가장 긴 줄 너비, T로 클릭)거나 `autoHeight`(너비 고정·높이 따라감, T로 끌거나 손잡이로 너비를 잡으면 이것이 된다)이고, 옛 파일의 nil은 autoHeight로 읽는다. 글 입력은 Figma처럼 배경 없는 `NSTextView`가 글자만 보이고 카드(채움·테두리)는 `SketchInputView.draw`가 그 아래에 그린다. 새 스타일 필드(`strokeHidden`·`cornerRadius`·`fontSize`·`textAlign`)와 원소 필드(`parent`·`name`·`clips`·`layout`·`textSizing`)는 **값이 있을 때만 쓴다**(둘 다 `encode(to:)`를 직접 구현) — 이 빌드가 그냥 열었던 파일은 바이트까지 그대로 다시 써지고, Portable은 모르는 키를 `extra`로 들고 다닌다. Portable도 같은 나무를 편집한다(`shared/sketchTree.ts`가 `SketchTree.swift`의 포팅, `ui/sketchInput.ts`·`ui/sketchInspector.ts`·`ui/sketchToolbar.ts`; 계약은 `ui/sketchEditing.ts`). 카드의 수식은 MathJax(`renderer/sketchMath.ts`, tex→svg→이미지, 비동기 로드 뒤 다시 그림). 열린 논문·핀·나란히(`shared/split.ts`)·⇧⌘O 팝업(`ui/openPapers.ts`)·논문 창(`--papertime-paper=<id>`로 같은 렌더러를 solo로)도 있다. 어느 쪽에 기능을 더하면 다른 쪽에도 같은 커밋 안에서 더한다.
- **그리기 층의 나머지 규칙.** 도구 줄의 아이콘은 SF Symbols가 아니라 `FigmaIcon`이 직접 그린다(1.6pt 선, 둥근 이음). "끝" 단추는 없다 — 읽는 중에 그린 것(원소나 스트로크)을 클릭하면 `installMarkClickMonitor`의 `hitsDrawing`이 `mode = .draw`로 바꾸고 `updateCanvasInteraction()`을 **즉시** 불러 그 클릭이 오버레이에 떨어진다. 그린 것의 인스펙터는 떠다니지 않고 창의 인스펙터 **Tools 탭**(`InspectorTab.tool`, `SketchInspector(docked: true)`)이며 펜을 들면 그 탭이 앞으로 온다; 인스펙터 탭 이름 넷(Info·Marks·Notes·Tool)은 시스템 언어와 무관하게 영어다. 카드의 `$…$`는 `SketchTypesetter.mathProvider`가 있을 때만 수식으로 조판된다 — 맥은 `MathBridge.install()`이 `MathTypesetter`를 꽂고(iOS·Portable·PDF 사본은 쓴 그대로), 수식이 든 문단은 줄바꿈 없이 한 줄이다. 글꼴은 `SketchStyle.fontName`(패밀리 이름; `SketchTypesetter.font(points:name:)`가 그 패밀리가 실제로 있을 때만 쓴다). 모서리는 `Corner` 척도로만 쓴다: 둥근 것 안의 둥근 것은 `Corner.inner(outer, inset:)`(바깥 반경 − 간격)로 동심원을 만든다 — 같은 반경을 안팎에 주면 안쪽이 더 날카롭게 보인다. 다크 모드의 Glass는 아래 «쪽 색조» 항목을 본다. `--papertime-dark=1`이 창만 다크로 보여 주고, `--papertime-split=1`이 처음 두 논문을 나란히 놓는다.
- **쪽 색조는 «바탕 + 그리는 법»이다(0.9.10부터).** 다섯 가지(`PageTint`: 종이 흰색·세피아·어둡게·유리·직접 고른 색)가 각자 바탕(`groundColor`, 유리와 종이 흰색은 없음 = 패널)을 갖고, **그리는 법은 바탕이 정한다**(`rendering`): 인쇄 그대로(`plain`), 곱하기(`multiply` — 종이의 흰색이 바탕으로 빠지고 잉크가 남는다), 밤(`night`). 밝은 바탕(휘도 ≥ 0.5)은 곱하기, 어두운 바탕은 밤 — 검은 잉크를 어두운 바탕에 곱하면 아무것도 안 남기 때문이다. 유리는 라이트면 곱하기, 다크면 밤이다(한때 다크의 유리는 그냥 흰 종이였다). **밤은 옵시디언의 «Adapt to theme»처럼 읽힌다**: PDF 뷰 레이어에 `CIColorInvert`+`CIHueAdjust(π)`(밝기만 뒤집고 색상은 되돌린다)를 걸고, 그 결과를 `screenBlendMode` 합성 필터로 바탕에 **스크린**한다 — 뒤집힌 종이의 검은색이 바탕 속으로 사라져서 쪽의 테두리가 없고 잉크는 밝다. 그러려면 **뷰의 바탕(쪽 사이 틈)도 불투명 흰색**이어야 한다: 예전 «어둡게»는 투명이었고 반전 필터 밑에서 쪽 사이가 흰 띠로 나왔다. 사진은 `FigureOverlayView`가 **미리 뒤집어** 그려서 레이어의 반전을 지나 인쇄된 그대로 나온다 — 그리고 그 사진을 찾는 `PageImages`에 버그가 있었다: 폼 안으로 들어갈 때 `CGPDFContentStreamCreateWithStream`의 둘째 인자에 폼의 `/Resources`가 아니라 스트림 사전을 넘겨서, **폼 안의 이미지는 하나도 못 찾았다**(TeX에 PDF로 넣은 그림이 전부 폼이다 — 코퍼스에서 998개만 찾고 495개를 놓쳤다; V-JEPA 2의 그림 1은 0개). 크기 문턱도 40×24pt에서 12×12pt로 내렸다 — 영상 프레임 격자처럼 작은 사진 여럿으로 된 그림이 흔하다(코퍼스 2,786개). 사용자 색은 `readerTintColor`(`#rrggbb`)에 두고, 읽는 화면이 뷰의 `@AppStorage`로 들어서 **색상 패널을 끄는 동안 쪽이 따라온다**(`ReaderConfiguration.adoptCustomTint`) — 모델 객체가 `UserDefaults` 알림을 받게 했더니 Swift 6가 «sending 'self'»로 막았다. 메뉴에서 «직접 고른 색»을 고르면 시스템 색상 패널이 열린다(`GroundColorPanel`; 메뉴 안에는 색 우물을 못 둔다). 설정 화면도 같은 두 키를 뷰의 `@AppStorage`로 읽는다 — `AppSettings`는 `@ObservationIgnored`라 그 값에 걸린 줄이 바로 안 나타난다. **이 합성은 사진으로 확인할 수 없다**: `--papertime-window-shot`은 `cacheDisplay`라 레이어 필터를 못 담고, 새로 넣은 `--papertime-window-shot-server=1`(창 서버에서 제 창만)은 **스테이지 매니저가 켜진 이 맥에서 숨긴 앱의 창을 기울어진 썸네일로** 돌려준다(`com.apple.WindowManager GloballyEnabled = 1`), 앱 밖에서 `CARenderer`를 돌려 보면 빨간 바탕조차 0을 낸다. 그래서 확인은 둘로 한다: `--papertime-tint=<이름>`(+`--papertime-tint-color=#rrggbb`, 설정에 안 쓴다)이면 `applyTint`가 레이어에 실제로 건 것을 한 줄로 찍고(`tint: dim rendering night ground #26272b filters ["CIColorInvert", "CIHueAdjust"] compositing screenBlendMode background 1,1,1,1`), 모양은 같은 필터 순서를 Core Image로 실제 쪽 그림에 적용해 본다(스크래치 `tint/scan.swift preview`). 포터블도 같은 모델이다(`shared/pageTint.ts`). 데모: `ReleaseNotes.Demo.nightPage`.
- **옮기기는 Figma처럼 붙고, 복제하며 끌 수 있다(0.9.9부터).** 선택을 옮기는 동안 그 상자의 두 변과 가운데(축마다 셋)가 페이지의 다른 원소와 **쪽 자체**의 같은 여섯 값 중 가장 가까운 것에 화면 6pt 안이면 붙고, 붙은 동안 1px 빨간 정렬선이 두 상자를 잇는다(쪽이면 쪽 전체). 기하는 창 없이 시험되는 순수 함수다 — `SketchSnap.adjust`(InkEngine)와 그 포팅 `shared/sketchSnap.ts`가 **같은 일곱 문제**를 받는다. ⇧를 **끄는 도중에** 누르면 큰 쪽 축으로 묶이고, 묶인 축에서 찾은 정렬선은 버린다(움직이지 못하는 정렬선은 거짓말이다). ⌥나 ⌘(윈도우·리눅스는 Ctrl)를 끄는 도중 처음 보는 순간 선택의 사본(하위 나무째, 새 id)이 **원래 자리에** 남고 원본이 계속 끌려간다 — 사본은 끄는 동안 입력 층이 그리고, 놓을 때 옮기기와 **한 undo**로 쪽에 들어간다(한 번 되돌리면 사본이 사라지고 원본이 돌아온다). 사본은 정렬 대상이 아니다: 출발점으로 끌어당기는 자석은 도움이 아니다. 다른 쪽으로 옮기면 사본은 원래 쪽에 남는다. 함께 선택한 **펜 선도 보정된 offset으로** 옮긴다 — 포인터 offset으로 옮기면 도형이 정렬에 내준 몇 pt만큼 선택이 둘로 갈라졌다.
- **모션은 `Motion` 척도로만 쓴다.** 움직임이 한 앱의 움직임으로 읽히려면 같은 크기의 변화가 어디서나 같은 시간을 써야 한다. 자리마다 적으면 그렇게 안 된다 — 여든 몇 군데에 서로 다른 지속시간이 열일곱 개 있었고, 그래서 포인터 밑에서 칩이 켜지는 것과 칸 하나가 통째로 들어오는 것이 같은 속도였다. 사다리는 `Corner`와 같은 모양이고, 칸은 **창의 얼마가 움직이는가**로 고른다: 손에 답하는 컨트롤은 `Motion.tap`(0.14), 패널 안에서 움직이는 것은 `Motion.move`(0.22), 면이 통째로 들고 나는 것은 `Motion.surface`(0.32), 아무도 안 보는 채로 혼자 꺼지는 것은 `Motion.fade`. 앞의 셋은 곡선이 하나다(`snappy`) — 곡선은 목소리이고 앱의 목소리는 하나다. Portable도 같은 사다리를 CSS 변수로 갖는다(`--motion-tap`/`--motion-move`/`--motion-surface`/`--motion-fade`, `--ease`는 그 스프링을 베지에로 적은 것). **시스템이 움직임을 줄여 달라고 하면 따른다** — 맥은 `Motion.watch()`가 `NSWorkspace`의 설정을 듣고 스프링을 짧은 이즈로 바꾸고, Portable은 `prefers-reduced-motion`이 같은 변수를 바꾼다. 변화는 그대로 일어난다(아무것도 난데없이 나타나지 않게), 다만 아무것도 이동하거나 튕기지 않는다.
- **환영 시트는 한 번에 하나씩 보여준다.** macOS의 «새로운 기능»과 같은 모양이다: 위에 이름, 큰 회색 카드 안에 실물 데모와 그 아래 제목·문단, 카드 밖 아래에 점, 좌우에 화살표(←→ 키도 넘긴다). 스무 가지를 한 줄로 세워 두면 아무도 안 읽는다 — 목록이 아니라 보여주는 것이기 때문이다. `FeatureShowcase` 하나를 환영 화면과 About이 같이 쓴다. 창 없이 확인: `--papertime-whats-new=1`(+`--papertime-window-shot=<경로>`). 이 프로브는 **버전을 봤다고 표시하지 않는다**.
- **라이브러리는 폴더 여럿이다(0.9.1부터).** 첫 폴더가 처음 고른 폴더일 뿐, 본진은 아니다 — **폴더의 것은 전부 그 폴더 안에 있다**. 기록도, 노트도, 그 논문들이 입은 태그와 컬렉션도. 그래야 폴더 하나를 다른 기계로 가져갔을 때 온전하고, 여기서 연결을 해제했을 때 딱 그 폴더만 빠진다(한때 노트·태그·컬렉션을 첫 폴더에 모아 두었는데, "내 노트는 결국 각 PDF에 대한 노트인데 왜 남의 폴더에 사느냐"가 맞는 말이었다). 맥은 `LibraryModel.sources`/`store(for:)`가 논문이 어느 폴더 것인지 보고 그 스토어로 저장하고(경로 접두사, 가장 긴 것이 이긴다), Portable은 `ownerOf(id)`가 같은 일을 한다(`snapshot()`이 id→폴더를 채운다). 태그·컬렉션은 **정의를 여러 폴더에 같은 id로 복제**한다: 읽을 때 id로 합치고(`mergeVocabulary` / `vocabulary()`), 쓸 때는 그 이름을 입은 논문이 있는 폴더마다 정의를 넣는다(`settleVocabulary`). 이 되메우기는 읽을 때마다 돌고 이미 갖춰져 있으면 아무것도 쓰지 않으므로, 옛 라이브러리의 이사이자 "폴더가 빠져 있는 동안 붙인 태그"의 수선이다. 노트도 같다 — `NotesModel`이 상자를 여럿 읽고(`SlipBox` 프로토콜: 폴더마다 `LibraryStore` 하나, 그리고 앱의 `LooseNotes` 하나), 제 논문이 다른 폴더에 있는 노트는 **두 폴더가 다 열려 있을 때만** 옮긴다(`settle()`). **논문 없는 노트는 폴더에 두지 않는다** — 그냥 떠오른 생각도, 지도도, 초안도 갈 폴더가 없으니 앱의 제 폴더(`Application Support/Notes`, 평범한 .md 묶음)에 산다. 대가는 그 상자가 기기 사이를 안 따라간다는 것이고(클라우드 폴더가 아니니까), 얻는 것은 어느 폴더를 뽑아도 안 사라진다는 것이다. 사이드바 맨 위 «라이브러리» 구역이 폴더별 보기(`Scope.folder(URL)` / `shelf.kind === 'folder'`)이고, 우클릭 «연결 해제»는 **목록에서만 뺀다** — 파일도 기록도 그대로 두는 것이 이 앱의 약속이다. 새 PDF는 지금 보고 있는 폴더로 들어가고(`importDestination`), 폴더 안에 이미 있던 PDF는 **그 폴더가** 들여온다(옮기지 않는다). 폴더마다 감시자가 하나씩 붙는다. 기억은 맥이 보안 스코프 북마크 배열(`libraryExtraBookmarks`), Portable이 `settings.extraRoots`다 — 안 열리는 폴더(뽑힌 디스크)는 조용히 빼고 다음에 다시 붙인다. 창 없이 확인: `--papertime-add-folder=<경로>`, `--papertime-remove-folder=<경로>`, 그리고 **`--papertime-folders=1`**(폴더마다 논문 수·태그·컬렉션·노트를, 그리고 앱 상자의 노트를 stderr에 찍는다 — 폴더가 온전한지 보는 유일한 길).
- **논문 없는 노트는 사람이 고른 폴더에 산다(0.9.9부터).** 설정 → 라이브러리 아래 «논문 없는 노트»에서 폴더를 고르면(`AppModel.chooseNotesFolder`, 보안 스코프 북마크는 `LibraryLocationPreference.storeNotesFolder`) 노트를 **옮긴다**(`LooseNotes.move(into:)`): 상자가 곧 노트라서, 옮기지 않고 가리키는 곳만 바꾸면 사용자에게는 노트가 사라진 것이다. 같은 이름이 이미 있으면 **덮지 않는다** — 그 노트는 있던 자리에 남아 계속 읽히고, 설정이 몇 개가 어디 남았는지 말한다(바이트가 같으면 옮긴 것으로 친다). 쓰는 중인 편집은 먼저 써서 함께 가고, 열려 있던 노트는 id로 계속 열려 있다. **고른 폴더에 닿지 못해도 선택을 잊지 않는다**: 그동안은 앱 폴더에 쓰고, 폴더가 돌아오면(다음 실행이나 «다시 연결») 옮긴다. 고른 폴더를 그 자리에 **다시 만들지 않는다** — 뽑힌 디스크 자리에 생긴 빈 폴더는 진짜 폴더가 영영 못 보는 노트 더미다. 떨어져 있는 동안 고친 노트는 같은 이름의 두 노트가 되고, 둘 다 두고 새것을 보여준다(자동 병합은 없다 — 읽은 판을 실행을 넘어 기억해야 안전하다). **시작할 때는 노트를 처음 읽기 전에** 상자를 정한다(`openNotes()`) — 잘못된 상자에서 먼저 읽지 않는다. 그리고 **프로브는 사용자의 노트 상자를 건드리지 않는다**: 프로브와 설치본은 컨테이너가 하나라, 전에는 `--papertime-library=`를 준 실행도 진짜 `Application Support/Notes`를 읽고 썼다. 지금은 앱 폴더가 프로브 것이고(`--papertime-notes-box=`, 없으면 라이브러리 옆 `<이름>-loose-notes`), 저장된 선택은 읽지도 쓰지도 않는다(`--papertime-notes-chosen=`이 그 자리에 선다). 창 없이 확인: `--papertime-notes-folder=<경로>`가 `notes folder: <경로> moved=N kept=M`을 찍고, `--papertime-folders=1`이 어느 폴더가 상자인지, `--papertime-settings=1`이 화면 밖 창에 설정을 그려 그 줄의 글을 찍는다. 폴더 고르기 패널 자체는 프로세스 밖에서 떠서 사람 손으로만 확인된다.
- **라이브러리는 한 갈래씩 열린다(0.9.3부터).** 폴더를 여럿 열 수 있게 되자 사이드바는 길어지기만 했다 — 한 학기를 폴더로 나눠 두면 논문보다 폴더가 먼저 스무 개가 된다. 그래서 라이브러리를 누르면 **나머지는 물러나고** 그 안의 폴더가 들여쓰여 서고, 지금까지 내려온 길이 자취로 남는다(`LibraryModel.folderTrail(to:)` / `state.ts`의 `folderTrail`). `Scope.folder`는 이제 **«이 폴더 아래 전부»**다(`isUnder` / `isUnderFolder`) — 학기를 누르면 학기 전부, 주차를 누르면 주차만. **나가는 길은 지금 눌린 그 줄**이다(`scopeRow(whenCurrent:)` / 같은 셸프를 다시 고르면 `folderAbove`): 들어갈 수만 있고 나올 수 없는 목록은 함정이고, 나가는 길은 손이 이미 얹힌 줄이어야 한다. 최상위에서 한 번 더 누르면 «모두». **트리는 디스크가 아니라 논문에서 읽는다**(`subfolders(of:)`) — 그래야 트리가 목록이 보여줄 수 있는 것과 정확히 같고(아무것도 없는 폴더는 가 봐야 빈 목록이다), 클라우드 폴더에 왕복을 한 번도 더 쓰지 않는다. 그리고 **종류 선반은 폴더마다 이름을 얹는다**(`visibleByFolder` / `papersByFolder`) — 종류 선반은 모든 폴더에서 긁어오는 것이 존재 이유라서, 예순 줄이 한 줄로 이어지면 어느 학기 것인지 알 수가 없다. 폴더가 둘 이상일 때만 얹는다. 창 없이 확인: `--papertime-scope=folder:<라이브러리 안 경로>`와 `--papertime-scope=papers|books|lectures|documents` — **SwiftUI 줄은 합성 클릭으로 못 누르고**, 창에 쏜 키는 앞에 있는 아무 앱에나 들어간다(실제로 두 번 남의 편집기로 들어갔다).
- **강의자료는 네 번째 종류다(0.9.3부터).** 책이 «인용하니까» 제 종류가 됐다면, 강의자료는 **«양이 많아서»** 제 종류다 — 한 학기가 슬라이드·노트·강의계획서 서른 개이고, 일반 문서에 두면 계약서와 매뉴얼 사이에 섞인다. 인용하지 않는다(`isCitable`은 논문과 책만). 짐작은 둘이고 **둘 다 논문 판정 뒤에** 온다: **쪽이 가로로 넓은 것**(종이로 읽으라고 만든 것은 가로가 아니고, 띄우라고 만든 것은 가로다 — 글자를 한 자도 안 읽고 되는 유일한 신호다. 두 쪽을 한 장에 인쇄한 논문이 걸리지 않게 초록·참고문헌 검사 다음이다)과 **이름**(`DocumentGuess.namesACourse` / `namesACourse` — 파일 이름이 첫 쪽보다 믿을 만하다. 데크의 첫 쪽은 그 주 제목만 적혀 있다. 과목 코드 `[a-z]{2,4}\s?-?\s?\d{3}`가 가장 세다). 답할 때 **아무것도 지우지 않는다** — 책은 내보내지니까 학술지 칸을 지우지만, 강의자료는 애초에 안 나가서 지워봐야 사용자가 적은 것만 잃는다.
- **선반은 세 묶음이다.** 사이드바의 선반이 묻는 것이 셋이라서다 — 무엇인지(모두·논문·문서), 얼마나 읽었는지(안 읽음·읽는 중·읽음), 내가 뭘 했는지(즐겨찾기·살펴볼 것·열린 논문). 한 줄로 아홉 개가 이어져 있으면 하나를 고르려고 아홉을 다 읽게 된다. 제목은 안 붙인다(세 줄 위의 이름은 이름이 필요 없는 것에 붙인 이름이다); 맥은 빈 헤더(`Color.clear.frame(height: 8)`)로, Portable은 빈 `.sidebar-section`(`gap()`)으로 사이를 벌린다. **빈 선반을 숨기지 않는 이유**: 읽기 상태 줄들은 논문을 끌어다 놓는 자리이고, 없는 줄에는 못 놓는다.
- **사이드바 맨 위는 라이브러리 목록이다.** 창 위에 «라이브러리 PaperLibrary»라고 한 줄 있던 자리인데, 라이브러리가 하나가 아니게 된 순간 그 줄은 거짓말이 됐다. 지금은 열어 둔 폴더가 줄마다 서고, 이름이 그 칩(연한 강조색 바탕 + 실선 테두리, `FolderName` / `.folder-name`)을 입는다 — 노트 속 인용 구절과 같은 모양이고, 같은 이유다: 어디서 온 것인지를 말한다. 아이콘은 클라우드와 이 기계를 가른다(`CloudProvider.symbolName` / `shared/cloudProvider.ts`의 `providerOf`+`providerIcon`; Portable 쪽은 윈도우의 `G:\My Drive`와 `OneDrive - 회사`까지 안다).
- **파일 이름은 인스펙터에서 고친다.** 라이브러리는 사람이 이름 붙인 PDF들의 폴더이므로 앱에서 보는 이름과 Finder에서 보는 이름은 같은 이름이어야 한다. `LibraryStore.rename(_:to:)`(Portable은 `Library.rename`)가 파일만 옮긴다 — 기록 폴더는 논문의 UUID라서 표시·잉크·노트·읽기 상태는 움직이지 않는다. 확장자는 안 적어도 붙이고(`.pdf`가 아니게 된 PDF는 데스크톱이 안 여는 PDF다), 구분자가 든 이름·빈 이름·이미 있는 이름은 거절한다(`RenameFailure` 넷; **말은 앱 쪽에서 고른다** — 패키지는 어느 언어로 읽히는지 모른다). 대소문자만 바꾸는 이름은 대소문자를 안 가리는 디스크에서도 되게 따로 뚫어 둔다. **열려 있는 세션을 반드시 데리고 간다**: 세션은 제가 연 경로로 표시를 되쓰므로, 먼저 `flush()`하고 옮긴 뒤 `documentMoved(to:meta:)`로 알린다. 누가 열고 있는지는 `DocumentSession.open(forPaper:)`(약한 정적 등록부)가 안다 — 창이 여럿이어도 전부 잡힌다. 창 없이 확인: `--papertime-rename=<이름>`.
- **들여오지 못한 파일을 목록에서 지우지 않는다.** «이 폴더에 남은 PDF n개 더하기»가 눌러도 눌러도 안 사라졌다. `adopt`가 건네받은 URL을 **전부** `looseDocuments`에서 빼고 있었기 때문이다 — 기록이 생겼는지는 보지 않고. 그래서 못 읽은 파일은 목록에서 사라졌다가 폴더를 다시 읽는 순간 돌아왔고, 누른 사람에게는 «눌렀는데 아무 일도 안 일어난다»로 보였다. 지우는 것은 **실제로 기록이 생긴 것만**, 그리고 **파일 이름이 아니라 전체 경로로** 뺀다(이름으로 빼면 `2026-1학기/slides.pdf`를 들여오면서 `2026-2학기/slides.pdf`도 같이 지워진다). 못 들여온 것은 `adoptFailures`에 담아 줄 아래에 «PDF n개는 더하지 못했어요»로 **말한다** — 클라우드 파일이 아직 안 내려온 것이 가장 흔한 이유라서, 그 말이 곧 «조금 뒤에 다시»다. 그리고 들여오는 동안은 단추 자리에 진행 표시가 서고 수는 스물다섯 개마다 줄어든다(이백 개를 들이는 내내 같은 숫자가 서 있으면 사람은 한 번 더 누른다). 들여오기는 **한 번에 하나만** 돈다 — 폴더를 감시하는데 들여오기가 그 폴더를 바꾸므로, 사람이 누른 것과 감시자가 답한 것이 같은 목록을 들고 함께 들어올 수 있다. 창 없이 확인: `--papertime-adopt-loose`에 `--papertime-folders=1`을 더하면 `loose: n [...] refused: n [...]`이 찍힌다.
- **폴더에 이미 있던 PDF는 바이트가 같아도 제 기록을 갖는다.** 같은 바이트라는 것은 «이건 이미 들여왔다»는 답이고, 그 답은 **밖에서 들어오는 파일**에 대한 것이다 — 같은 내려받기를 두 번 끌어다 놓는 것을 막는다. 폴더 안에 이미 있는 파일에 그 답을 하면, 라이브러리가 보여주지 않고 보여주게 만들 수도 없는 PDF가 폴더에 남는다: 위의 단추가 영영 안 사라지던 두 번째 이유가 이것이었다. 폴더에 사본이 둘이면 목록에도 둘이다(`importDocument`의 `alreadyInside`). 대신 **이미 기록이 있는 파일**은 다시 들이지 않는다 — 그 검사는 모델이 한다(`importDocuments`의 `claimed`), 패키지는 어느 경로가 이미 임자가 있는지 모르기 때문이다.
- **붙일 논문은 목록이 아니라 찾기로 고른다.** «다른 논문에 붙이기»가 제목 순 서른 편짜리 하위 메뉴였다. 찾는 것은 보충자료의 부모이고, 그건 A에 있을 확률과 Z에 있을 확률이 같다 — 이백 편짜리 라이브러리에서는 대개 그 서른 안에 없었고, 메뉴는 없다는 말조차 못 한다. 지금은 시트(`AttachSheet`)가 열리고 제목과 **파일 이름** 양쪽으로 찾는다(`Karmanov_…_CVPR_2024.pdf`를 받은 사람은 어느 제목에도 없는 «karmanov»를 친다). 순위는 `AttachmentSearch`에 있고 창이 없어도 시험된다: 통째 일치 → 제목 안 → 파일 이름 안 → 낱말 앞머리(«adapt»가 «adaptation»을, «강화학습»이 «강화학습의»를 찾아야 한다) → 하나도 안 맞으면 제목 전체 유사도로 오타를 건진다. 열릴 때 추천은 **스스로 보충자료라고 적힌 문서에만** 단다 — 앞머리가 같은 두 제목은 Jaro-Winkler로 쉽게 0.82를 넘고, «이 논문 같아요»라는 머리말 아래의 틀린 답은 답이 없는 것보다 나쁘다(실제로 «Scene-Graph ViT…»에 «Scene Graph Generation by Iterative Message Passing»을 달았다). 창 없이 확인: `--papertime-attach=<말>`이 후보 수·추천·상위 열 편을 찍고 시트를 연다.
- **시트는 제 창이다.** `--papertime-window-shot`이 `NSApp.keyWindow`를 찍는데, 시트가 떠 있으면 그 밑의 창이 찍혀서 시트가 사진에 없다. `window.attachedSheet`가 있으면 그쪽을 찍는다 — 환영 시트든 설정이든 붙이기든 마찬가지다.
- **목록이 길어지면 느려지는 것은 줄을 재기 때문이다.** 실측(Release, 안정된 라이브러리): 한 걸음이 270편에서 36ms, 600편에서 86ms. 우리 코드는 한 걸음에 **아무것도 안 만든다**(행 본문 0, 행 구조체 0, 화면 아래 뷰 352개 고정). 스택이 가리키는 곳은 `-[NSTableView layout]` → `_uncachedAutomaticRowHeight` → `NSISEngine`(오토 레이아웃) → `NSHostingView._willUpdateConstraintsForSubtree` → `ViewGraphRootValueUpdater._sizeThatFits` — **줄 하나를 재는 데 그 줄의 SwiftUI 뷰 그래프를 통째로 한 바퀴 돌린다.** «처음 한 번만 비싼 것»이 아니다: 같은 마흔 자리를 다섯 번 훑어도 84.6·85.8·86.9·87.8·87.4ms로 평평하다(스크롤 시험이 왕복마다 중앙값을 찍는 이유). 세 가지를 재 봤고 셋 다 N에 비례한다 — `List` 86ms, `ScrollView`+`LazyVStack` 89ms(`LazyVStack`도 전체 높이를 알아야 해서 자식을 다 잰다), 줄마다 `.frame(height:)`를 준 `List` 46ms(절반이지만 여전히 비례). **N에 비례하는 항을 없애려면 N개를 배치하지 않는 컨테이너여야 한다** — 그래서 맥의 가운데 칸은 `PaperTable`(우리가 가진 `NSTableView`)이다: 보이는 줄만 뷰를 만들고, 전체 높이는 덧셈이고, 높이는 우리가 준다. 실측 14.2ms(270편)·23.2ms(600편), 왕복마다 평평하다. 줄 높이를 하나로 묶는 것은 답이 아니다: 줄은 제목 한두 줄에 부제와 태그라서, 한 높이로 묶으면 반쯤 빈 줄들이 된다(찍어서 확인했다).
  - **높이는 그 줄 자신에게 물어 캐시한다.** 공식을 세워 두면 줄의 배치가 바뀔 때마다 따로 고쳐야 하고, 한 번 어긋나면 글자가 잘린다. 그래서 실제로 보여줄 SwiftUI 뷰를 한 번 세워 `fittingSize`를 묻고, 그 답을 «무엇이 높이를 정하는가»로 캐시한다(제목·부제 유무·태그 수 — 별이나 읽기 상태는 높이를 안 바꾼다). 재는 자는 **하나를 만들어 돌려 쓴다**: 처음 열 때 육백 줄을 각각 새 `NSHostingView`로 재면 그게 한 걸음 270ms다(자를 돌려 쓰면 98ms). 칸 너비가 바뀌면 전부 버린다 — 좁아지면 한 줄이던 제목이 두 줄이 되고, 그때가 다시 재야 하는 유일한 때다.
  - **줄이 SwiftUI면 클릭은 표까지 안 내려간다.** 표로 바꾸자 우클릭은 되는데 **왼쪽 클릭이 아무 일도 안 했다** — 그래서 논문이 안 열리고 키보드도 같이 죽었다. 계측으로 본 것: `table hitTest → NSHostingView`가 1, `table mouseDown`이 **0**. 줄은 `NSHostingView` 안의 SwiftUI라서 클릭을 그쪽이 먼저 먹고, 우클릭만 되던 것은 줄의 `.contextMenu`가 받아서였다. 그러니 **줄이 스스로 고른다**: 탭을 받아 선택을 고치고 표를 first responder로 만든다(그래야 화살표가 갈 곳이 생긴다). 수식키는 **주변 상태에서 읽지 않는다** — `NSApp.currentEvent`도 `NSEvent.modifierFlags`도 닫힘이 실행될 즈음엔 그 이벤트가 끝나 있다. `TapGesture().modifiers(.shift)`·`.modifiers(.command)`를 `highPriorityGesture`로 걸어 **SwiftUI가 이벤트에서 갈라 주게** 한다(실측: 3번 → 1개, ⇧6 → 4개, ⌘4 → 3개, 9번 → 1개).
  - **줄을 누르는 것은 창 안에서 확인한다.** `--papertime-click-row=3;6,shift;4,command;down;up`이 표의 그 줄을 누르고(⇧·⌘·화살표까지) 고른 개수와 지금 보이는 논문을 찍는다. 이벤트는 **이 앱 안에서 만들어 이 창에만** 보내므로(`window.sendEvent`) 시스템 큐로 나가지 않는다 — 포터블이 페이지 안에서 이벤트를 만드는 것과 같은 이유다. 걸림돌 하나: **숨긴 앱의 창은 visible이 아니고, AppKit은 안 보이는 창에 마우스 이벤트를 안 준다** — 그래서 클릭 전에 `NSApp.unhideWithoutActivation()`으로 숨김만 푼다(창은 이미 모든 디스플레이 밖이라 아무것도 안 보인다). 이걸 모르고 한참을 «고침이 안 먹는다»로 읽었다.
  - **줄은 여전히 SwiftUI다.** `PaperRow` 그대로이고, 우클릭 메뉴도 그 안의 `.contextMenu`다 — `NSTableView`가 제 메뉴를 먼저 올리므로 `PaperTable.Table.menu(for:)`가 눌린 줄을 고르고 이벤트를 아래로 흘린다. 드래그는 `pasteboardWriterForRow`가 **SwiftUI가 쓰던 바이트 그대로** 쓴다(앱 고유 타입에 `PaperTransfer`의 JSON) — 그래야 컬렉션·읽기 상태·나란히 놓기의 드롭 자리를 하나도 안 고친다. 구역 제목·«남은 PDF» 줄·본문 검색 결과는 전부 `Entry`로 같은 표에 들어간다(한 스크롤 뷰가 다 이고 간다). 아래로 당겨서 찾기는 `boundsDidChange`로 직접 알린다 — 스크롤 시험이 끝에 한 번 당겨 보고 팔레트가 열렸는지 찍는다. **아이패드·아이폰은 `List` 그대로다**(수십 편이고, 유지할 목록이 둘이 되는 값을 못 한다).
- **시작할 때 클라우드 폴더를 걸어 다니지 않는다.** 여는 데 2초가 걸리던 이유는 `FileOperations.ensureDownloaded`가 **기다릴 파일이 이미 와 있는데도** 폴더 전체를 재귀로 훑어 파일마다 내려받기 상태를 묻고 있었기 때문이다(실측 1942–2385 ms → 20 ms). 기다릴 파일이 있고 통째로 내려와 있으면 그 자리에서 돌아오고, 남은 것은 `requestPendingDownloads`로 **기다리지 않고** 부탁한다. 클라우드 폴더에서는 `stat` 하나, 디렉터리 목록 하나가 다 왕복이다 — 라이브러리 안을 도는 반복문을 쓰기 전에 그 반복문이 몇 번 왕복하는지 센다.
- **시작할 때 라이브러리는 한 번만 읽는다.** `addSource`는 폴더를 더하고 `refresh()`를 부르므로, 시작하면서 옆 폴더를 하나씩 더하면 라이브러리를 폴더 수만큼 읽는다(폴더 셋이면 세 번, 그중 둘은 버려진다). 그래서 시작 경로는 `attachSource`로 조용히 들이고 끝에 한 번 읽는다. 폴더를 여는 것(`bootstrap`)과 읽는 것(`loadAll`)은 둘 다 **폴더마다 동시에** 한다 — 하나가 깨어나기를 기다리는 동안 다른 폴더가 서 있을 이유가 없다. 노트 상자도 같다.
- **이사는 한 번만 돈다.** `migrateNotesIntoSlipBox`는 노트를 읽을 때마다 처음부터 다시 돌면서 기록 폴더마다 `stat` + 디렉터리 읽기 + 삭제 시도를 했다 — 논문 80편이면 왕복 240번, 매번 "할 일 없음"을 알아내려고. 지금은 다 옮기고 나면 폴더의 `.papertime/notes-moved`에 표를 남긴다. 표는 **폴더 옆에** 둔다(기계가 아니라 폴더가 이사한 것이고, 표를 지우면 한 번 더 도는 것 말고는 잃는 게 없다). 읽을 때마다 도는 마이그레이션을 쓸 때는 "이미 끝났다"를 한 번의 `stat`으로 답할 수 있게 해 둔다.
- **느린 자리는 계측이 가리키는 곳이지 짐작하는 곳이 아니었다.** 이 최적화에서 고친 것 중 목록 인덱스·태그 조회 같은 "당연히 느릴 것 같던" 것들은 실제로 6 ms였고(`library: index 79 papers`), 진짜 2초는 위의 두 항목이었다. `Trace.time`을 먼저 심고 재라. 그리고 **클라우드 폴더 위에서 잰 숫자는 같은 빌드끼리도 1.4초와 2.8초 사이에서 흔들린다** — 그 위에서 잰 한 번의 측정으로 좋아졌다/나빠졌다를 말하지 않는다.
- **프로브는 아무것도 기억하지 않는다.** 앱은 번들 id마다 샌드박스라서 프로브와 사람이 쓰는 사본이 **같은 설정 도메인**을 쓴다. `--papertime-library=<경로>`가 주어진 실행은 그래서 고른 폴더를 저장하지 않고(`open(location)`을 직접 부른다, `adopt(folderAt:)`가 아니라), 옆 폴더도 저장하지 않으며(`rememberExtras`), 남의 옆 폴더를 열지도 않는다. Portable도 같다(`isProbeLibrary()`). 두 번 어겼고 두 번 다 사용자의 라이브러리 설정을 테스트 폴더로 덮었다.

- **핀과 열린 선반은 다른 것이다.** 열린 논문 선반은 **앱이** 관리한다(쓰면 남는다 — 쪽을 클릭하면 `link.activated` → `keepOpen`). 목록 줄 앞의 핀은 **사람이** 하는 것이고, `keepOpen(_:byHand:)`가 `true`로 불릴 때만(핀 단추, 줄 메뉴의 "열어 두기", 나란히 놓기, 새 창) 켜진다. 한때 둘이 같았고, 그래서 논문을 읽기만 해도 핀이 켜졌다 — 핀은 누가 누르는 것이지 저절로 켜지는 것이 아니다(`pinnedPaperIDs`, Portable은 `store.pinnedPaperIDs`).

- **논문 나란히 보기.** `LibraryModel.openPaperIDs`는 **훑어본 논문이 아니라 쓴 논문**이다 — 목록을 내려가며 보이는 것마다 쌓으면 선반이 목록과 같아진다. 클릭해 들어가거나(`link.activated` → `keepOpen`), 나란히 놓거나(`dock`), 메뉴의 "열어 두기"로 남고, 지금 보이는 것은 "미리보기"로 선반 끝에 잠깐 있다가 다음 논문을 보면 빠진다(편집기의 미리보기 탭과 같은 규칙). 칸의 ×와 선반의 ×는 논문을 닫는다(`undock` + `closeOpenPaper`). (`Scope.open`이 "열린 논문" 선반)와 `AppModel.split: SplitArrangement?`(왼쪽 열·오른쪽 열, 열마다 위·아래 — 최대 넷). 놓는 자리는 `DockZone` 여섯 개이고, 쪽 영역에 `DockDropDelegate`가 `PaperTransfer` 드롭을 받아 `DockZoneOverlay`로 자리를 미리 비춘다(가운데 40%는 자리가 아니다). 비춘 자리는 마우스 단추가 떼어지면 지운다(`NSEvent.pressedMouseButtons` 감시) — Escape로 취소한 드래그나 창 밖에서 놓은 드래그는 `dropExited`를 보내지 않아 반쪽이 파랗게 남았었다. 우클릭 메뉴 "나란히 열기"도 같은 `app.dock`. 각 칸은 제 `ReaderScreen`이고, **포커스된 칸만 창의 `ReaderLink`를 빌려 쓴다**(나머지는 `app.paneLink(for:)`) — 그래서 툴바·인스펙터·노트가 그 논문 것이 된다. 그러려고 `ReaderCoordinator.link`가 `var`이고 `updateNSView`가 매번 다시 넣으며, `ReaderScreen`은 링크 객체가 바뀌면 제 세션을 새 링크에 다시 `adopt`한다. 칸 클릭은 클릭 모니터가 `link.activated?()`로 알린다. ⇧⌘O(`.openPapers`)는 `OpenPapersPopup`을 차례 팝업처럼 쪽 위에 띄운다; 줄 뒤의 `DragOutHandle`(NSDraggingSource)이 클릭·⌘클릭·드래그를 받고, 드래그가 아무 데도 받아들여지지 않고 우리 창 밖에서 끝나면(`draggingSession(_:endedAt:operation:)`, operation 비어 있음) `WindowGroup(id: "paper", for: UUID.self)`의 새 창(`PaperWindow`)을 그 자리에 연다. 새 창은 제 `ReaderConfiguration`·`ReaderLink`를 가지고, ⇧⌘D 알림은 키 윈도우일 때만 받는다.
- **윈도우·리눅스 빌드(`Portable/`).** 맥이 아닌 데스크톱에는 PDFKit도 PencilKit도 없어서, 코드를 옮기는 길은 없고 다시 쓰는 길만 있었다. 그래서 **코드가 아니라 파일을 공유한다** — 라이브러리는 PDF 옆에 작은 JSON이고 표시·필기는 PDF 안에 있으니, 그 파일들에 합의한 두 구현은 그 자체로 호환된다(서버도, 프로토콜도, 마이그레이션도 없다). 두 번째 구현은 Electron + TypeScript다: 윈도우와 리눅스가 **똑같이** 보여야 한다는 요구를 정직하게 지키는 길은 크로미움 하나뿐이고, Qt나 GTK는 유지할 외양이 둘 더 느는 일이다. 대가는 패키지당 100 MB 남짓.
- **두 빌드 사이를 건너는 것.** 레코드(`.papertime/*.json`)는 바이트까지 같다 — Swift의 `JSONEncoder`는 콜론 앞에 공백을 두고, 키를 UTF-8 바이트 순으로 정렬하고, 빈 배열을 `[`·빈 줄·`]`로 쓰고, 닫는 괄호에서 개행 없이 끝난다. `Portable/src/shared/coding.ts`가 그걸 그대로 낸다(틀려도 눈에는 안 보이고, 대신 건드린 파일마다 클라우드 폴더에서 전체 변경으로 보여 조용한 동기화가 충돌이 된다). **날짜 모양이 둘**인 것에 주의: 레코드는 `JSONCoding`의 `.iso8601`, 스케치 사이드카와 `/PTSketch`는 기본 `JSONEncoder`의 2001-01-01 기준 초다. 게다가 `Date`는 밀리초까지만 쥐므로 맥이 쓴 `811492215.409792`를 다시 인코딩하면 `…4089999`가 된다 — **파일에 있던 숫자를 그대로 보관했다가 그대로 쓴다**(`createdAtRaw`). 도형은 `/PTSketch`가 원소 JSON을 싣고 다니므로 사이드카 없는 기기가 파일만으로 똑같이 되살린다: 실제로 사이드카를 전부 지운 PDF를 맥에 줬더니 맥이 **바이트까지 같은** `sketch/p0000.json`을 써냈다. BibTeX도 출력이 같다(Swift 내보내기와 직접 대조했다). LaTeX 이스케이프 표는 옮겨 적지 않고 `Portable/tools/generate-latex-table.mjs`가 `LaTeXEscaping.swift`에서 **생성**한다 — 손으로 옮기면 "Almudévar"가 기기마다 다르게 나온다.
- **클라우드 폴더와 Finder에서 건네받기.** 라이브러리는 어느 클라우드 폴더든 된다 — 맥에서 Google Drive는 `~/Library/CloudStorage/GoogleDrive-<메일>/`(File Provider)이고, 샌드박스 앱은 **사용자가 건네준 뒤에만** 그 안을 본다. 건네는 길은 둘: 열기 패널(`chooseLibraryFolder`)과 **Finder에서 앱에 끌어다 놓기**(`OpenWithFinder`, `CFBundleDocumentTypes`에 `public.folder`). 둘 다 Launch Services/Powerbox가 같은 권한을 주고, 보안 스코프 북마크로 다음 실행에도 열린다(실측: Drive 폴더를 떨어뜨린 뒤 인자 없이 다시 띄우니 그 폴더가 열렸다). Portable은 샌드박스가 없어 경로만 있으면 된다. 폴더를 못 읽으면 `CloudProvider`가 어느 서비스인지 말해 준다.

- **논문과 책과 일반 문서.** 0.9.0부터 라이브러리는 여러 가지를 담는다: `PaperMeta.kind`(`DocumentKind`: `.paper`/`.document`)가 **사용자의 답**, `guessedKind`가 **앱의 짐작**이고, 둘 다 없으면 `effectiveKind`는 `.paper`다(옛 라이브러리가 그대로 보이도록). 둘 다 값이 있을 때만 파일에 쓴다. 짐작은 `DocumentGuess.of(hasIdentifier:hasAbstract:hasReferences:)` — DOI/arXiv가 보이면 논문, 없으면 초록**과** 참고문헌이 함께 있을 때만 논문이다. 참고문헌은 40쪽 이하면 전부, 그보다 길면 끝 8쪽 + 열두 곳을 본다(부록 있는 논문은 참고문헌이 가운데 있다 — 실측: 23쪽 논문의 9쪽). 맥은 들여올 때 PDFKit으로 짐작하고(`DocumentSignals.guess`), **짐작이 문서면 등록기관에 묻지 않는다**(계약서 제목을 밖으로 보내지 않는다). Portable은 pdf.js가 창에 있으므로 논문을 처음 열 때 짐작한다(`shared/documentKind.ts`, `reader.ts`의 `guessKindIfAsked`). 인스펙터는 맨 위에서 **먼저 묻되 한 번만 묻는다**(`kindQuestion` — 답하면 사라진다. 한 논문의 일생에 한 번 정하는 것이고, 다시는 안 건드릴 스위치를 폼에 세워 두지 않는다). 바꾸는 길은 도구 막대의 ⋯ 메뉴와 목록의 우클릭 메뉴 두 곳이다 — 되돌릴 길이 없으면 그건 함정이니까. 답에 따라 칸이 갈린다: 논문이면 학술지·권·호·DOI·인용 키와 후보 목록, 문서면 펴낸 곳·해·종류·파일. `살펴볼 것`과 BibTeX의 "확인 안 됨" 경고는 논문만 센다. 사이드바의 `논문`/`문서` 줄은 라이브러리에 둘 다 있을 때만 나온다.
- **줄은 낱말 안에서 끊지 않는다.** 한국어에는 낱말 가운데서 줄을 끊으면 안 된다는 규칙이 없어서, 브라우저는 자리가 모자라면 아무 데서나 끊는다 — «라이브러리»가 «라이브 / 러리»가 되고, 읽는 사람이 낱말을 도로 붙인 다음에야 문장을 읽는다. 문장마다 고칠 수 있는 것이 아니므로(어느 문장의 성질이 아니다) **한 번, 문서 전체에** 건다: 배포 페이지는 `html { word-break: keep-all; overflow-wrap: break-word }`, Portable도 같은 두 줄(`html, body`). `keep-all`이 끊을 자리를 띄어쓰기로 옮기고, `break-word`는 그 아래의 안전판이다 — 제 칸보다 넓은 토큰(경로, 주소, 긴 합성어)은 어딘가에서는 끊어야 한다. 영어는 영향이 없다(이미 띄어쓰기에서만 끊는다). **맥은 SwiftUI `Text`가 이미 낱말 단위로 끊는다**(실측: 낱말이 칸보다 넓을 때만 안으로 들어간다) — 그래서 맥에서 고칠 것은 SwiftUI가 아닌 두 곳이었다. TextKit(노트 편집기·글 카드 편집기)은 `NSParagraphStyle.lineBreakStrategy`가 기본값 `[]`이라 낱말 안에서 끊었고(`.standard`로 바꾸면 띄어쓰기에서 끊고, 낱말이 칸보다 넓으면 글자로 끊는다), Core Text(`SketchTypesetter`)는 **문단 스타일의 이 값을 아예 보지 않는다** — `CTFramesetter`도 `CTTypesetterSuggestLineBreak`도 무시한다(실측). 그래서 카드는 Core Text가 고른 자리를 받아서 **낱말 안이면 마지막 띄어쓰기로 되돌린다**(Latin의 규칙은 Core Text가 아는 대로 두고, 줄에 띄어쓰기가 없으면 원래 자리를 쓴다). 끊을 자리는 개행으로 심어 넣고 조판은 그대로 framesetter에 맡긴다 — 하드 개행과 소프트 개행의 행 높이가 같아서, 줄이 안 옮겨간 카드는 갖고 있던 높이를 그대로 갖는다.
- **선반의 종류 줄은 셋이 한 벌이다.** 라이브러리에 종류가 둘 이상 있으면(그때만) 줄이 나오고, 그때는 **비어 있는 것까지 셋 다** 나온다. 종류는 늘어나는 목록이 아니라 한 질문에 대한 세 개의 답이고, 둘만 서 있으면 앱이 둘만 안다는 말이 된다 — 책이 빠져 있던 것이 하필 책이 있다는 걸 알아야 할 라이브러리들이었다.
- **환영 시트는 가장 긴 쪽에 맞춰 자리를 잡는다.** 카드 아래 글자 칸은 모든 쪽의 글을 겹쳐 쌓아 두고 하나만 보여준다(`ZStack` + `hidden()`) — 그래서 칸의 높이가 언제나 «가장 긴 쪽»의 높이이고, 쪽을 넘겨도 아무것도 움직이지 않는다. 그 자리에 숫자를 적어 두면(적혀 있던 것은 116이었다) 누가 더 긴 문장을 쓰는 순간 낡는다. 시트 높이도 고정하지 않는다 — 680×700이었고, 글이 길어진 쪽에서 마지막 줄들이 점 아래로 흘러나갔다. 글자 칸이 쪽마다 같아졌으니 시트는 내용의 높이를 그대로 쓰면 되고, 그러면 잘릴 수가 없다. «정보»는 설정 창에서 **제 크기를 갖는다** — 나머지 칸은 폼이라 760×540이 넉넉하지만 «정보»는 실물 크기 데모에 그 아래 문장이라 540에서는 카드 위쪽이 창 밖으로 잘렸다. macOS의 설정도 보여주는 칸에 맞춰 크기를 바꾼다. **좌우 화살표는 포인터에 답한다** — 회색 화살표 둘이 카드 옆에 그냥 서 있으면 눌리는 것인지 모른다(실제로 처음 본 사람이 그랬다). 답하는 방식은 앱이 이미 쓰는 것 그대로다 — `pressable`(`Color.primary` 0.055의 옅은 바탕 + 손 커서). 제 바탕을 따로 그려 봤더니 화살표 뒤에 회색 덩어리가 앉았다: **카드 옆에 선 컨트롤이 카드보다 무거우면 안 된다.** 갈 곳이 없는 화살표는 답하지 않는다.
- **책은 세 번째 종류다(0.9.2부터).** 책은 일반 문서가 아니다 — **책은 인용하기 때문이다.** 교과서를 논문 칸에 넣으면 같은 제목의 학술지 논문에 잘못 붙어 «권 9, 호 5, 1054–1054쪽»이 되고(실제로 Sutton & Barto가 그렇게 잡혔다), 일반 문서 칸에 넣으면 인용이 아예 안 된다. 그래서 `DocumentKind`는 셋이고 술어가 둘이다: `isCitable`(문서만 false — BibTeX에 나가는가)과 `isLookedUp`(논문만 true — 등록기관에 물을 수 있는가). 책의 칸은 출판사·펴낸 곳·판·해·ISBN이고 학술지·권·호·쪽·ISSN은 답하는 순간 **지운다**(`setKind`) — 남겨 두면 `@book`에 권과 호가 딸려 나간다. CSL 타입은 `.book`(혹은 사람이 고른 `.chapter`)으로 바꾸고, 그래서 BibTeX·인용 서식은 손댈 게 없었다 — `Bibliography`는 이미 책을 알고 있었다. 짐작은 **길이 + 참고문헌**이다(`DocumentGuess.bookLength = 100`쪽): 길이만으로는 스캔한 매뉴얼이 걸리고, 참고문헌만으로는 초록 없는 짧은 논문이 걸린다. `살펴볼 것`은 여전히 논문만 센다(책에는 따질 등록기관이 없다). 사이드바의 종류 줄은 **둘 이상 있을 때만, 있는 것만** 나온다. 창 없이 확인: `--papertime-kind=<paper|book|document>`(맥), Portable은 줄의 우클릭 메뉴가 지금 아닌 종류 둘을 보여준다.
- **쪽 그림(page grid).** 차례(⇧⌘L)에 `차례`/`쪽` 두 칸이 있고, 제목을 하나도 못 찾으면 `쪽`으로 열린다(`ContentsPopup.Mode`). 썸네일은 행이 보일 때 그리고 그 뒤로 캐시한다(`PageThumbnail`) — PDFKit 문서는 두 스레드에서 못 읽으니 메인 액터에서 그리되 `Task.yield()`로 한 번에 몰리지 않게 한다. Portable은 `ui/pages.ts`(같은 키, `IntersectionObserver`로 늦게 그림, `Reader.thumbnail`이 pdf.js로 작은 캔버스를 만든다). Portable에는 차례(제목 읽기)가 없어 `쪽`만 있다.

- **잠긴 PDF.** PDF 암호화에는 핸들러가 있고, 표준(`/Standard`)만 암호로 열린다. 나머지 둘 — 인증서(`Adobe.PubSec`)와 회사 권한 서비스(`MicrosoftIRMServices` 등) — 는 열쇠를 남이 들고 있어서 이 앱이 열 길이 없다(Acrobat이 여는 것은 MIP 플러그인을 갖춘 등록된 앱이기 때문이다). 판정은 `PDFLock`(PaperCore, `shared/pdfLock.ts`가 포팅): 핸들러 이름은 암호화될 수 없어 파일에 평문으로 있으므로 바이트에서 찾고, **그 이름만으로는 아무것도 아니다** — 문서가 안 열리거나 잠겨 있거나, 열렸는데 앞 세 쪽에 글자가 하나도 없을 때만 그 핸들러의 것이다(권한 관리를 다룬 논문이 있다). PDFKit은 모르는 핸들러의 파일을 **거절하지 않고** 열어서 암호문을 그린다 — `isLocked`도 `isEncrypted`도 false다. 그래서 글자 유무를 본다. 암호가 걸린 파일은 맥이 `document.unlock(withPassword:)`, Portable이 pdf.js의 `task.onPassword`로 연다(**아무도 안 물어보면 pdf.js의 약속은 영영 안 끝난다** — 0.8.1까지 빈 화면이던 이유). 잠긴 파일에서는 서지 추출을 건너뛴다(암호문이 제목이 된다). 그리고 `paper:bytes`는 절대 던지지 않는다 — 렌더러에서 unhandledrejection이 되면 화면에 아무 말도 안 남는다.

- **«Invalid PDF structure»는 진단이 아니라 한 마디다.** pdf.js는 그 문장을 딱 한 자리에서 던진다 — xref로 못 읽어 파일 전체를 다시 훑었는데 `/Root`가 없을 때. 뜻은 하나다: **건네받은 바이트가 온전한 PDF가 아니다.** 잘린 파일, 0으로 채워진 클라우드 자리표시자, PDF 이름을 단 HTML 로그인 페이지, 권한 에이전트가 남긴 OLE/zip 컨테이너가 **전부 같은 문장**으로 떨어진다(반대로 헤더를 망가뜨리거나 가운데 50%를 뭉개거나 startxref 오프셋이 엉터리인 파일은 그냥 열린다 — 실측). 그래서 **구분은 pdf.js에게 묻기 전에** 한다: `shared/pdfLock.ts`의 `looksLikePDF`(첫 1 KB의 `%PDF-` — 0바이트가 아니라 1 KB인 이유는 앞에 배너가 붙은 파일도 pdf.js가 멀쩡히 열기 때문)·`looksWhole`(마지막 2 KB의 `%%EOF`)·`containerKind`(OLE `D0CF11E0A1B11AE1` 또는 `PK\x03\x04`이면서 `%PDF-`가 없음)·`diagnose`. **판정은 문이 아니라 무슨 말을 할지 정하는 방아쇠다** — pdf.js는 꼬리가 통째로 없는 파일도 복구해서 여니, 잘린 파일도 결국 넘겨주고 말만 바꾼다. 그리고 `fs.promises.readFile`은 **짧은 버퍼를 조용히 돌려준다**(실측: 96 MB 파일에서 32 MB, 에러 없음). 맥에는 `FileOperations.ensureDownloaded`가 있고 Node에는 클라우드 필터 API가 없으므로, 수화를 부르는 유일한 방법인 «다시 읽기»를 유계로 돈다(`readWhole`, 250 ms × 5, 고칠 수 있는 종류에만 — 컨테이너와 웹 페이지는 몇 번을 읽어도 같다). 실측: 내려받는 중에 연 파일이 0/10 → 10/10. **컨테이너는 잠긴 파일이다** — 브랜드는 알아내려 하지 않는다(모양은 누가 쌌든 같고, 본 적 없는 제품명 목록은 지어낸 증거다). 그리고 **컨테이너에서 pdf-lib이 던지면 `rightsHandler`를 반드시 부른다** — `PDFLock.swift`의 `guard let document else { return rightsHandler(in: data).map(.rights) }`가 포팅에서 빠져 있었고, 그래서 Acrobat이 잘 여는 회사 파일이 «파일이 깨졌을 수 있어요»로 떴다. 들여올 때의 복사도 `.part`로 받아 `rename`한다(같은 폴더 안 rename은 반쪽을 못 남긴다) — 복사 중에 그 논문을 누르면 **앱이 스스로** 이 오류를 만든다.

- **텍스트 층에는 `--scale-factor`를 줘야 한다(포터블).** pdf.js가 만드는 글자 span은 전부 `font-size: calc(var(--scale-factor) * Npx)`이다. 그 변수가 없으면 `calc()`가 무효라 선언이 통째로 버려지고, **모든 span이 창의 기본 글자 크기를 물려받는다.** 실측: 6.97px·9.96px·14.35px를 요청한 span이 전부 13px로 나왔고, 논문 한 편 6,471개 run 중 6,465개가 같은 높이였다. 자리는 퍼센트로 잡히니 글자는 제자리에 보이고, 그래서 아무도 눈치채지 못한 채 **선택 영역의 사각형만 전부 틀렸다** — 상자가 줄 간격보다 1.6배 높고(줄 간격/상자 = 0.54~0.69), 잉크는 상자의 0.07~0.42에만 있고, 쪽 밖으로 넘치는 span이 페이지마다 수십 개였다. `PageView.layout`이 `--scale-factor`를 `scale`로 넣는다. 넣고 나면 상자는 진짜 em 상자다: 잉크가 0.06~0.79, 줄 간격/상자 = 1.10, 넘치는 span 0개. **표시 사각형을 손으로 깎아 맞추지 마라** — 0.62를 곱해 두었던 것은 이 버그를 가린 값이었고, 변수를 넣자 26개 문단 전부가 줄 수만큼 정확히 quad를 냈다(그 전에는 21개).
- **표시는 글자의 줄을 따라간다(포터블).** 맥은 PDFKit의 `selectionsByLine()`이 줄을 주지만 여기서는 선택의 `getClientRects()`가 조각마다 사각형을 주고, 그것을 줄로 묶는 일은 이쪽이 직접 한다(`shared/textLines.ts`). **묶는 기준은 그 줄을 연 조각의 상자 하나이고 절대 누적이 아니다** — 누적이면 조금 큰 조각 하나를 삼킨 순간 아래 줄에 닿고, 닿으면 그 다음 줄에도 닿는다. 실측: 한 쪽에서 34줄이 5줄로(그중 하나는 글자 높이의 8.5배), 참고문헌 8줄이 1줄로 묶여서, 사용자 화면에는 반 단을 가로지르는 노란 덩어리가 나왔다. 기준값은 «짧은 상자의 0.6이 겹칠 것»이고, 논문 세 편에서 같은 줄 6,920쌍·다른 줄 733쌍·크기가 다른 같은 줄 748쌍(윗첨자·아랫첨자)을 재서 그 사이에 놓았다(같은 줄 p05 0.78~0.88, 첨자 p05 0.63~0.74, 다른 줄은 아예 안 겹친다). 더 올리면 윗첨자가 제 줄에서 떨어져 나와 하이라이트 위에 얼룩으로 보인다. 그리고 **글자보다 훨씬 큰 조각은 줄을 만들지도, 줄을 높이지도 않는다** — 제가 걸친 줄들을 옆으로 넓힐 뿐이다(첫 글자 장식). 걸친 줄이 하나도 없으면(끌다가 들어온 제목) 그때만 제 줄이 된다. 대가는 적어 뒀다: 여러 줄에 걸친 큰 기호의 위쪽은 덮이지 않는다. 자라는 줄은 이 버그 자체라서, 모자란 띠가 삼키는 띠보다 작은 잘못이다.
- **표시는 제 캔버스에 그리고 그 캔버스를 `mix-blend-mode: multiply`로 쪽에 곱한다(포터블).** 캔버스 안에서 `globalCompositeOperation = 'multiply'`를 주면 아래에 아무것도 없는 면 위에서 곱하는 것이라 그냥 불투명한 노란색이 되고, 형광펜이 글자를 지운다. 맥이 같은 자리에서 먼저 겪고 `MarkOverlayView`에 적어 둔 것과 같다(레이어의 `compositingFilter`). 펜과 도형은 그 면을 쓰지 않는다 — 잉크는 물감이고, 흰 도형은 흰 채로 있어야 한다. 쪽 캔버스·표시·찾기·필기는 `div.page-print` 한 상자 안에 있고, 그 상자가 쌓임 맥락이라 표시는 바탕이 아니라 **쪽 위에서** 곱해진다 — 색조는 그 상자를 통째로 섞는다(아래 항목).
- **쪽 색조는 바탕 하나와 그리는 방식 하나다(포터블, 맥과 같은 모델 — `shared/pageTint.ts`).** 이름 다섯(`none`·`sepia`·`dim`·`glass`·`custom`)과 색 하나(`pageTintColor`, `#rrggbb`, 기본 `#26272b`)이고, 그리는 방식은 이름이 아니라 **바탕이 정한다**(`renderingFor`, 단위 시험): 종이 흰색은 인쇄된 그대로(`plain` — 흰 쪽과 그림자), 세피아(`#f5eddb`)와 밝은 직접 고른 색(상대 휘도 ≥ 0.5, 회색이면 `#bcbcbc`부터)은 **곱하기**, 어둡게(`#26272b`)와 어두운 직접 고른 색은 **밤**, 유리는 제 바탕이 없어 패널 색 위에서 밝은 화면이면 곱하기·어두운 화면이면 밤이다(`data-theme`을 보고, `applyTheme`이 불릴 때마다 모든 리더를 다시 칠한다). 곱하기가 어두운 바탕에서 안 되는 이유: 검은 글자를 어두운 색에 곱하면 아무것도 안 남는다. 옛 이름은 **읽을 때** 바꾼다(`grey`→`none`, `night`→`dim`, `settings.ts`의 읽는 길과 `update`) — 옛 «밤»은 파란 회색을 흰 쪽에 곱한 것이라 회색 위의 검은 글자였다. **바탕은 쪽이 아니라 읽는 칸 전체다**: 곱하기와 밤에서는 `.reader-scroll`이 바탕색이고(유리면 투명 — 패널의 `--panel`이 보여서 다른 패널들과 같은 색이 된다) `.page`는 종이도 그림자도 없다. 쪽 사이 틈이 종이와 같은 색이어야 쪽이 탁자 위의 카드로 안 읽힌다. 섞기는 `.reader-panel[data-rendering]`의 CSS 두 줄이다: 곱하기는 `.page-print { mix-blend-mode: multiply }`(종이의 흰색이 바탕으로 빠지고 잉크가 남는다), 밤은 `filter: invert(1) hue-rotate(180deg)`(1 − M·c — 휘도만 뒤집고 색상은 둔다) 뒤에 `mix-blend-mode: screen`(뒤집힌 종이의 검정이 바탕으로 빠진다 — 쪽 가장자리가 없다; 필터가 섞기보다 먼저 걸린다). 상자 뒤에 흰 바탕을 두고(아직 안 그린 쪽도 종이다), 쪽 캔버스는 크기를 잡자마자 흰색으로 칠한다 — 막 잡은 `alpha: false` 캔버스는 pdf.js가 배경을 칠할 때까지 검정이고, 밤에는 그 검정이 흰 번쩍임이다. 치는 중인 카드 글(`sketch-text-editor`)과 선택 색도 밤에 맞춘다.
  - **밤에도 사진은 인쇄된 그대로다.** 상자 밖, 글자 층 아래의 `canvas.image-canvas`가 쪽 캔버스에서 그림 자리만 복사한다 — 필터는 화면으로 가는 길에 걸리지 캔버스의 픽셀에 걸리지 않으니 쪽 캔버스는 늘 인쇄된 그대로다. 그 캔버스는 그림들을 합친 상자만 한 크기로 쪽에 대한 비율로 놓는다(사진 한 장 때문에 쪽 크기 캔버스를 한 장 더 쥐지 않는다; v-jepa 2 2쪽은 쪽 캔버스 832×1076에 그림 캔버스 217×158 px). 자리는 pdf.js가 **방금 그린 operator list**를 걸어 찾는다(`shared/pageImages.ts`, 순수 함수: `save`/`restore`/`transform`, 폼·투명 그룹·주석 외형의 시작과 끝, `paintImageXObject`·`…Repeat`·인라인과 그 묶음을 각자의 행렬 아래 단위 정사각형으로; 스텐실 마스크는 글자처럼 뒤집히는 게 맞아서 빼고, 12×12pt 이하(그림으로 그린 가는 선)도 뺀다 — 맥 `PageImages.swift`와 같은 문턱이다. 처음엔 둘 다 40×24였는데, 영상 프레임 격자처럼 작은 사진 여럿으로 된 그림이 흔해서 그 하나하나가 음화가 됐다). 목록은 `page._intentStates`에 남은 것을 먼저 쓰고 없을 때만 `getOperatorList()`를 부른다: 그걸 부르면 pdf.js가 쪽을 다시 읽고 그림을 **디코딩한 채 한 번 더** 넘겨서 쪽이 열려 있는 내내 두 벌을 쥔다. 그린 목록이라 캔버스에 있는 것과 정확히 같다. 코퍼스 58편×최대 60쪽(1,101쪽)을 MuPDF `get_image_info`와 대 보면 1,030쪽이 같고, 나머지는 크롭 상자 원점(MuPDF는 크롭 상자 기준이다, 62쪽), **패턴 속 그림**(6쪽 — pdf.js가 패턴 IR로 따로 그려 목록에 없다; 맥도 패턴은 안 걷는다), MuPDF가 그림으로 세는 셰이딩(1쪽 — 그라디언트 막대는 벡터라 뒤집히는 게 맞다)뿐이다. 그림 위의 필기는 그림 캔버스에 다시 얹는다: 표시는 곱해서(종이 위에서처럼), 펜·도형은 밤으로 바꿔 바탕에 screen한 뒤 제 알파로 되돌려서 — 그래야 사진을 가로지르는 선이 끝까지 한 색이고 흰 카드가 사진 위에서도 바탕색이다(그냥 얹으면 검정이다).
  - **잉크만 있는 흰 그림은 그림이 아니다(`isInkOnWhite`).** 스캔한 교과서(Casella & Berger — 686쪽이 쪽마다 그림 하나, 글자 0)가 밤에 흰 카드의 기둥이 됐다. 그래서 쪽이 그려진 뒤 그림마다 쪽 캔버스에서 64×64 **점**을 떠서(평균이 아니다 — 평균하면 글자 줄이 회색 톤이 된다) 종이(luma ≥ 224) 60% 이상·톤(96–224) 20% 이하·색(채널 차 ≥ 64) 5% 이하면 쪽과 함께 밤으로 보낸다: 스캔한 글, 선 그림, 흑백 차트는 뒤집혀서 주변 글처럼 읽힌다. 실측: 교과서 본문 블록은 종이 78–94%·톤 1–12%·색 0, v-jepa 2 그림 1의 연회색 로봇 사진은 종이 32%·톤 67% — 처음 규칙(밝은 것 70%·색 없음)은 이 사진을 잉크로 보고 음화로 만들었다. 흰 여백을 품은 사진은 여백째 흰 상자로 남는다 — 사진 속의 흰색과 여백을 가를 길이 없다. 이 규칙은 맥에 아직 없다.
  - **색 고르기는 손을 따라간다.** 설정 시트의 `<input type="color">`는 `input`마다 쪽을 다시 칠하되 **시트를 다시 그리지 않는다** — 다시 그리면 색 우물이 포인터 밑에서 떼어져 나가고 피커도 같이 닫힌다. 파일은 손이 멈추고 300 ms 뒤에 쓰고(`setSettings(…, { live: true })`), `change`에서 시트를 다시 그린다. 우물은 직접 고른 색일 때만 서고, 밝으면 세피아처럼·어두우면 어둡게처럼 그린다는 한 줄이 따라온다(회색 하나가 쪽을 밤으로 넘기면 고장으로 보이니까). 다섯 이름은 영어로는 라벨 옆 칸에 안 들어가서(380 px, 칸은 346 px — 한국어는 315 px) 두 말 모두 라벨 아래 줄 전체에 놓는다(`set-row-stacked`) — 말에 따라 모양이 바뀌는 행은 한쪽에서만 확인된다. ⋯ 메뉴와 시트가 같은 `setSettings`를 부른다.
  - 창 없이 확인: 프로브의 `window.__papertimeSettings.set({ pageTint, pageTintColor, appearance })`(시트와 같은 길이라 프로브에서는 메모리에만 남는다)와 `window.__papertimeSettings.tint()`(렌더링, 바탕, 쪽마다 그림 자리·출처 `drawn|asked`·톤·판정). 스크롤 비용(처음 연 논문 240프레임): 밤에서 34 ms 넘는 프레임 4개(최대 52 ms), 인쇄 그대로 5개(최대 46 ms), 두 번째 훑기는 둘 다 0 — 튀는 프레임은 쪽을 처음 그리는 값이고, 섞기 자체는 프레임을 잃지 않는다.
- **논문이 놓인 자리를 이유 없이 다시 짓지 않는다(포터블).** 스크롤 되는 칸은 문서에서 떼였다 붙으면 맨 위로 돌아간다. `reconcileReaders()`가 `clear(pageArea)` 뒤에 리더를 도로 넣고 있었고, 라이브러리는 폴더가 바뀔 때마다 다시 읽히고, 폴더는 표시를 저장할 때마다 바뀐다(`marks:save`가 저널을, 1.5초 뒤 `flushToPDF`가 PDF를 쓴다 — 둘 다 라이브러리 폴더 안이다). 그래서 9쪽에 하이라이트를 하면 0.5초 뒤에 1쪽이었다(실측: 1856 → 0). 지금은 배치의 모양(`shape`)이 실제로 달라졌을 때만 다시 짓고, 다시 지을 때는 각 리더가 읽던 자리를 기억했다가 되돌려 놓는다 — 나란히 놓인 칸 하나가 바뀌었다고 옆 칸이 처음으로 돌아가면 안 되니까.
- **왜 못 여는지는 바이트가 아니라 글자로 보여준다.** 첫 8바이트를 16진수로 찍어 두었더니 회사 컴퓨터(스크린샷 금지)에서 «3C 23 23 20 4E 41 53 32»가 전화로 불려 왔고, 그게 `<## NAS2`라는 건 손으로 풀어 본 뒤에야 알았다. 그래서 `headLine()`이 **읽을 수 있는 첫 줄을 그대로** 보여준다(UTF-8로 풀어 한국어 안내문도 읽히고, 제어문자가 하나라도 있으면 글자가 아닌 것으로 본다). 16진수는 개발자가 grep할 것이라 옆에 같이 두고, 크기는 1 KB 미만이면 바이트로 적는다 — 249바이트짜리 쪽지를 «0 KB»라고 하면 반올림 오차로 읽힌다. 실제 사례에서 그 줄은 원본이 있는 자리를 가리키는 쪽지였다: 파일 자리에 논문이 아니라 **쪽지**가 있고, Acrobat만 원본을 받는다. **쪽지가 가리키는 경로를 앱이 따라가지는 않는다** — 모르는 형식에서 경로를 읽어 여는 것은 넘지 말아야 할 선이고, 사람이 탐색기에서 한 번 열어 주면 되는 일이다(그래서 그 화면에 «폴더에서 보기»가 있다).
- **건너지 못하는 것: PencilKit의 `.drawing`.** 매직 넘버 뒤에 애플의 점 압축이 들어간 비공개 protobuf라 읽지 않는다. 필기는 PDF의 잉크 주석으로 건넌다(맥이 저장할 때 쓴다) — 잃는 것은 점마다의 압력인데, 그건 맥도 파일에서는 잃는 것이고 그래서 양쪽 다 제 사이드카를 따로 둔다. `.drawing`은 있는데 PDF에 잉크가 없는 쪽(맥에서 그리고 저장을 안 한 쪽)은 **아무 말 없이 빈 쪽을 보여주지 않고** 그렇다고 알린다. 반대로 그런 쪽에 여기서 그리면 맥의 파일을 `….drawing.superseded-<언제>`로 **이름만 바꾼다** — 사이드카 둘이 한 쪽을 주장하면 두 기기가 서로 다른 걸 보여주고(맥은 제 사이드카를 파일보다 먼저 읽는다), 지우지 않는 건 그 안의 압력이 사용자의 작업이기 때문이다.
- **일부러 다르게 둔 것은 창 단추 하나뿐이다.** 맥은 신호등을 왼쪽에, 윈도우·리눅스는 최소화·최대화·닫기를 오른쪽에 제 방식으로 그린다 — 그 단추는 데스크톱의 것이지 Paper Time의 것이 아니고, 윈도우에 초록 신호등은 버그로 읽힌다. 도구 막대의 양 끝 사이는 셋 다 같다. 글꼴은 **번들한 Pretendard**다("시스템 글꼴"은 기기마다 다른 글꼴이라 메모 너비가 달라진다). 아이콘은 `Portable/src/renderer/icons.ts`에 직접 그렸다 — SF Symbols는 애플 플랫폼 전용 라이선스다.
- **설정은 세 갈래로 열린다(Portable, 0.9.5부터).** 메뉴의 «설정…»과 `Ctrl+,`는 처음부터 있었지만 **그 뒤에 아무것도 없었다** — `runMenuCommand`에 `settings` 자리가 없었고 창에는 설정 화면 자체가 없었다. 맥은 별개의 앱이라 티가 나지 않았고, 그래서 윈도우·리눅스에서는 **말을 바꿀 길이 아예 없었다**(`settings.language`는 `main.ts`가 시작할 때 읽고 있었는데 그걸 건드릴 UI가 없었다 — 죽은 스위치가 아니라 닿을 수 없는 스위치였다). 지금은 도구 막대의 ⚙, 메뉴, `Ctrl+,` 셋 다 `ui/settings.ts`의 시트를 연다. **두 번째 창이 아니라 시트**다 — 창이 둘이면 윈도우와 리눅스에서 똑같이 보여야 할 크롬이 한 벌 더 늘고, 그게 이 빌드가 존재하는 이유다. 보고 시트(`ui/feedback.ts`)가 이미 그 틀을 정해 뒀으므로 같은 프레임에 줄만 다르다. **담는 것은 이 빌드가 실제로 바꿀 수 있는 것뿐이다** — 아직 없는 설정을 그려 두면 눌리지 않는 스위치가 되고, 그건 없는 것만 못하다. 읽기 설정(색조·배치)은 `⋯` 메뉴에도 그대로 둔다: 읽다가 손이 닿는 자리가 따로 있고, 맥도 도구 막대와 설정 양쪽에 둔다.
- **창 없이 확인하기(포터블).** 시스템에 이벤트를 쏘지 않는다: `--papertime-probe=<steps.json>`이 `eval`·`click`·`drag`·`key`·`menu`(메뉴 명령 — 단축키는 메인 프로세스의 가속기라 합성 키로는 안 눌린다)·`shot` 단계를 **페이지 안에서** 실행하고, `--papertime-shot=<png>`이 창을 찍는다(`rect`를 주면 그 영역만 — 나중에 자르는 건 엉뚱한 데를 보기 좋은 방법이다). `--papertime-library=<경로>`로 남의 라이브러리를 건드리지 않고 연다. `--papertime-chrome=win32`면 맥에서 윈도우 크롬을 그려 본다. 검사는 `cd Portable && npm test`(37개 — 대부분 "맥과 같은가"를 묻는다).
- **패키징.** `npm run dist:win`(NSIS + zip), `npm run dist:linux`(AppImage + tar.gz, x64·arm64)은 어느 OS에서든 된다. **`.deb`·`.rpm`은 맥에서 만들면 안 된다** — fpm이 GNU `ar`을 부르는데 맥의 `ar`은 Mach-O 아카이브를 만들고, 그 결과 96바이트짜리 패키지가 나오는데 electron-builder는 성공으로 보고한다. `tools/package.mjs`가 만들어진 것들의 크기를 재서 그런 게 나오면 실패시키고, `dist:linux-packages`는 리눅스가 아니면 아예 거절한다.
- **윈도우 설치 프로그램은 옛 버전이 막 지운 이름을 곧바로 쓰지 않는다(`Portable/assets/installer.nsh`, 0.9.10부터).** D:에 깔린 이전 버전 위에 0.9.8을 덮어 설치하자 «다음 파일을 열 수 없습니다»(NSIS의 `File`이 파일을 못 열 때의 말, 중단/다시 시도/무시)가 `uninstallerIcon.ico`에서 한 번, `Uninstall Paper Time.exe`에서 한 번 떴다. 업그레이드는 먼저 옛 제거 프로그램을 돌리고(`installUtil.nsh`의 `uninstallOldVersion`), 그것은 파일을 전부 `%TEMP%`로 **옮겨서** 치운다(`uninstaller.nsh`의 `un.atomicRMDir`) — 그런데 앱이 `%TEMP%`와 다른 드라이브에 있으면 옮기기는 복사+지우기이고, 누가 아직 쥐고 있는 파일(방금 돈 실행 파일을 보는 백신, 탐색기, 색인기)은 지워져도 **이름이 남는다**(목록에는 있고 열리지는 않는다). 아예 못 지운 파일은 제자리에 남는데 옮기기는 성공이라고 한다(`MoveFile`의 드라이브 간 이동이 그렇게 정의돼 있다). 새 설치 프로그램은 그 옛 이름 둘을 재시도 없는 `File`로 쓰고(아이콘은 옛 제거 프로그램 바로 뒤, 제거 프로그램은 맨 끝), 앱 파일은 재시도하는 복사로 들어가서 둘만 걸렸다. 그래서 셋을 한다: 아이콘 파일은 아예 안 쓴다(`uninstallerIcon`을 뺐다 — 제거 프로그램과 설정 → 앱의 아이콘은 실행 파일에서 오고 같은 그림이다). 옛 버전을 치운 직후(`customUnInstallCheck` — 이 훅은 electron-builder의 결과 처리를 **대신하므로** 그 처리를 그대로 옮겨 두었다) 제거 프로그램의 이름이 풀릴 때까지 20초까지 기다리고, 설치 끝(`customInstall`)에 제거 프로그램이 없으면 이름이 풀리는 대로 다시 쓴다(있고 읽히면 방금 쓴 것일 수 있으니 절대 지우지 않는다). 이 버전의 제거 프로그램은 무엇을 치우기 전에 제 파일을 **같은 폴더 안에서 이름만 바꾼다**(`customRemoveFiles`) — 한 폴더 안의 이름 바꾸기는 복사가 아니고, 쥐여 있는 파일도 이름은 바뀌므로 그 이름이 즉시 풀린다. 앞으로의 업그레이드는 그래서 기다릴 일이 없다(정책 때문에 `%TEMP%`가 아니라 제자리에서 돈 제거 프로그램은 제 `….old`를 못 지우는데, 그건 다음 설치의 `customInstall`이 치운다). `${isUpdated}` 같은 플래그 검사는 `$R9`를 덮어쓰니 제 상태는 이름 있는 변수에 둔다. **폴더 화면은 쓸 수 있는 폴더인지 먼저 묻는다**(`.onVerifyInstDir`): «나만» 설치는 관리자 권한 없이 돌아서 `C:\Program Files`에 못 쓰는데, 전에는 그대로 진행해 파일마다 실패하다가 앱 파일에서 «Paper Time을 닫을 수 없다»는 엉뚱한 말을 했다. 지금은 가장 가까운 있는 폴더를 추가 권한(`FILE_ADD_SUBDIRECTORY`, 설치 폴더가 있으면 `FILE_ADD_FILE`도)으로 열어 보고 — 아무것도 만들지 않으니 키마다 돌려도 된다 — Windows가 **«접근 거부»(오류 5)라고 답할 때만** 설치 단추를 끄고 페이지 첫 줄에 왜인지와 «모든 사용자»로 가는 길을 적는다 — 위로 있는 폴더를 못 찾거나(공유 폴더, 빠진 드라이브) 다른 오류면 막지 않고 NSIS와 설치에 맡긴다. 이 검사는 윈도우에서 한 번도 돌려 보지 못했고, 틀리게 «안 된다»고 하면 어디에도 설치할 수 없게 되기 때문이다(`?e`로 `GetLastError`를 받는다). 옛 버전이 남긴 `uninstallerIcon.ico`는 설치 끝에 지운다. 그리고 electron-builder가 `%LOCALAPPDATA%\paper-time-updater`에 남기던 설치 프로그램 사본(250 MB, electron-updater의 차분 업데이트용 — 우리는 안 쓴다, 제거해도 남았다)을 설치 끝에 지운다(electron-updater를 들이면 이 줄부터 빼야 한다). «C:에는 아예 설치가 안 된다»는 말은 메시지 없이는 못 가른다: 위의 Program Files 경우, Windows 보안의 랜섬웨어 방지(쓰는 순간 막으니 폴더 검사로는 안 잡힌다), C:의 공간(설치 프로그램은 앱 340 MB 말고도 `%TEMP%`에 압축본 130 MB와 푼 것 340 MB, 제 사본 250 MB를 C:에 쓰는데 NSIS의 공간 검사는 앱 크기만 센다)이 후보다. 파일은 `assets/`(electron-builder의 buildResources)에 둔다 — `Portable/build/`는 저장소의 `.gitignore`가 `build/`로 통째로 무시한다. 확인은 창 없이: 빌드가 `-WX`로 통과하고, `DEBUG=electron-builder`면 `dist/builder-debug.yml`에 남는 스크립트를 같은 인자로 makensis `-V4`에 다시 먹여 훅이 들어간 자리를 순서대로 본다(`-PPO`는 이 makensis에서 플러그인을 못 찾아 안 된다). makensis를 손으로 돌릴 때는 `LC_ALL=en_US.UTF-8`을 준다 — 맥의 makensis는 `-INPUTCHARSET UTF8`을 줘도 C 로케일에서는 `!include`한 UTF-8 파일의 한글이나 줄표가 든 첫 줄에서 «Bad text encoding»으로 멈춘다(electron-builder는 띄우는 프로세스마다 그 값을 스스로 준다 — `builder-util`의 `getProcessEnv`). 윈도우가 없으니 실제 덮어 설치는 사람 손으로만 확인된다.
- 플랫폼 분기: AppKit 전용 파일은 `#if os(macOS)`로 통째로 감싸고 UIKit 쌍을 옆에 둔다(`MarkOverlayView`, `MarginMaskView`, `PageOverlay`). 색은 `PlatformColorShims.swift`가 AppKit 이름을 UIKit에 준다.

- **표시는 PDF에 덧붙여 쓰고, 논문의 바이트는 다시 쓰지 않는다(0.9.9부터).** PDFKit의 모든 직렬화 — `dataRepresentation()`(옵션이 있든 없든), `write(to:)`, 아무것도 안 고친 문서까지 — 는 글꼴을 다시 서브셋한다. TeX 논문에서는 그것이 «ff» 합자를 «!»로 바꾸고(LeJEPA: «different» 0→23, «ff» 92→0), 코퍼스 63편 중 27편이 **첫 저장에서** 글자가 바뀌었다. 파일 크기가 두 배가 되는 것(8.7 MB→16.9 MB)이 그 증거다. 그래서 저장은 **증분 갱신**이다(`PDFUpdate` 타겟, `IncrementalWriter`): 새 주석만 든 버림용 문서를 PDFKit이 직렬화하고, 그 객체들을 원본 바이트 위에 옮겨 심어 xref와 `/Prev`가 있는 리비전 하나를 뒤에 붙인다. 원본은 xref 표든 xref 스트림이든 객체 스트림이든 RC4·AES 암호화든 직접 파싱한다. 바꿀 것이 없는 저장은 **0 B**를 붙이고(63/63), 대응할 수 없는 것(제자리에서 바뀐 객체, `/Annots` 중복, 끊긴 xref 사슬, 주석을 금지한 파일)은 **거절하고 표시를 앱에 둔다**(`keptInApp`, 발판의 조용한 한 줄) — PDFKit의 전체 재작성으로 되돌아가는 길은 없다. `DocumentSession.write`는 `appended/unchanged/keptInApp`을 돌려주고 `FileOperations.update`가 읽기-변환-쓰기 하나를 조정하며 **결과가 읽은 바이트로 시작하는지** 본다. 잉크는 스트로크마다 `/Ink` 하나다(한 획 저장이 400획을 다시 쓰지 않도록). 지문은 설명문이 아니라 주석 사전의 해시다(잉크 많은 쪽에서 저장 시간의 30%였다). 검사: `swift test --filter 'PDFUpdateTests'`가 합자 있는 TeX 픽스처(`Fixtures/ligatures.pdf`)를 왕복하고 코퍼스가 있으면 63편 × 4회 저장을 돈다; `swift run -c release papertime-pdfcheck protocol|text|growth`가 앱 밖에서 같은 일을 하고, `--papertime-mark-test-keep=<경로>`가 실제 앱에서 저장 네 번(표시·같은 것·지우기·같은 것)이 각각 몇 바이트를 붙였는지 찍는다. `PDFReader`·`PDFUpdate`·`App`에 `dataRepresentation`/`write(to:)`가 다시 들어오면 소스 검사 테스트가 잡는다. 끝까지 확인한 결과(P4, 2026-09-25): 코퍼스 63편 × 4회 저장 전부 통과하고 qpdf·pypdf·MuPDF·pdf.js가 새 문제 0, 실제 앱 7편(선형화·xref 스트림·RC4·AES-128 포함) 전부 «덧붙임/0/덧붙임/0», 복원은 8,818,759 B = 원본 8,758,154 + 꼬리, 200회 저장 파일은 압축으로 9.36→8.76 MB. **저널만 들고 온 표시도 파일에 쓴다**: `reconcile()`이 파일이 가진 것과 저널이 원하는 것을 비교해 다르면 보통의 지연 저장을 건다 — 전에는 여기서 무언가를 표시하기 전까지 다른 기기의 표시가 파일 밖에 남았다. 포터블도 이제 같은 방식으로 덧붙인다(아래 «포터블도 덧붙인다» 항목). `--papertime-compact=1`은 바탕을 아는 논문(`pdf/base.json` 또는 원본의 importDigest)에서만 «해야 할 때»가 된다 — 이력이 이미 든 파일을 새로 들이면 그 파일이 곧 바탕이다. 포터블 프로브의 `paper:bytes`는 우리 주석을 뺀 사본이라(`stripOwnedForDisplay`) 페이지 안 `getAnnotations()`로는 우리 표시가 안 세어진다 — `marks:load`로 센다. qpdf 실행 파일이 없으면 `pikepdf`(adversary venv)로 암호화 픽스처를 만든다.
- **포터블의 pdf-lib도 같은 세 가지를 지킨다.** `/Annots`는 `node.get`이 아니라 `lookup`으로 읽는다 — 간접 배열(참조)이면 `get`은 못 보고, 그래서 맥이 저장한 파일에서 표시 0개를 읽고 붙일 때는 배열을 **통째로 바꿔** 논문의 링크와 남의 표시를 지웠다(코퍼스 63편 중 37편, 맥이 저장한 파일은 63/63). 암호화된 파일에는 `ignoreEncryption`으로 평문을 쓰지 않는다 — qpdf·pdf.js·PDFKit 전부 쓰레기를 읽었다. 댓글 키는 맥과 같은 `/PTComment`다(`/PTMarkupIDComment`가 아니다).
- **포터블도 덧붙인다 — 논문의 바이트는 다시 쓰지 않는다(`Portable/src/main/pdfupdate/`).** pdf-lib의 `save`는 파일을 통째로 다시 써서, 글자와 주석은 온전해도 원본 바이트가 사라졌다 — 맥은 그 파일을 «다른 앱이 다시 썼다»(`rewritten`)로 분류하고 «원본 글자 되살리기»를 내밀었고, 접기는 바탕을 못 찾았다. 그래서 맥의 `PDFUpdate`를 **구조 그대로 옮겼다**: `syntax.ts`(객체 모델·렉서·파서·`spansOf`·직렬화·Flate+예측자), `file.ts`(xref 표·스트림·`/Prev`·`/XRefStm` 사슬, 객체 스트림, 복구 스캔 — 고쳐 읽은 것은 전부 `notes`·`guessedReads`에 적는다), `crypt.ts`(표준 보안 핸들러 RC4 40/128·AESV2·AESV3, R2–R6 — 읽을 때 풀고 새 객체는 그 파일의 열쇠로 잠근다), `appendix.ts`(파일의 최신 절과 같은 종류의 xref, `/Prev`, `/ID[1]` 갱신, 빈 목록), `canonical.ts`(`Canonical.form`·`isOurs`·`reach`의 포팅과 그 해시인 `fingerprint`), `writer.ts`(`Opened.open` → `annots` → `perform`: 쪽 사전은 파일의 제 글자에 `/Annots` 하나만 갈아 끼우고, 지운 주석의 객체는 빈 목록에 올리며, 결과를 제 리더로 다시 읽어 확인한다). **pdf-lib은 읽는 데도 안 쓴다** — 그 파서는 xref를 따르지 않고 파일을 처음부터 훑어 «마지막 것이 이긴다»로 읽어서, 사슬이 끊겼는지 알 길이 없다. `pdfwrite.ts`의 공개 API(`writeDrawings`·`readMarks`·`readDrawings`·`stripOwnedForDisplay`)는 그대로고 그 아래가 바뀌었다: 원하는 주석 집합은 전과 같이 저널·사이드카에서 만들고(형광펜·밑줄·`/PTComment`·잉크·`/PTSketch` 사본), 파일이 이미 가진 것과 **id로** 짝을 맞추되(도형은 `/PTSketch` 페이로드, 표시는 `sameMark`) 잉크는 **주석 사전의 지문**(`fingerprint`, 맥의 `AnnotationFingerprint`와 같은 뜻)으로 짝을 맞춘다 — 같은 획은 같은 바이트를 낳도록 숫자를 `n()`의 어휘 그대로 쓴다(`num`). 바꿀 것이 없는 저장은 **0 B**(`bytes`가 같은 객체로 돌아온다). 화면용 사본(`stripOwnedForDisplay`)도 같은 덧붙이기를 메모리에서 한다 — 그래서 암호화된 파일에서도 우리 표시가 두 번 그려지지 않는다. **거절하고 아무것도 안 쓰는 것**(`WriteRefused.reason`, 발판의 «표시는 Paper Time에만 있어요»는 이유별 말풍선): 열쇠를 모르는 핸들러·암호(`encrypted`/`needsPassword`), 주석을 금지한 `/P`나 `DocMDP<3`(`permissions`), 끊긴 사슬·스캔으로 찾은 객체·길이를 고친 스트림·`/Annots` 중복·되쓸 수 없는 쪽(`structure`), 결과가 읽히지 않음(`verification`). 전체 재작성으로 되돌아가는 길은 없다. 디스크에는 `appendVerified`가 놓는다: 같은 폴더의 임시 파일 → 다시 읽어 길이와 **앞부분의 SHA-256**이 원본과 같은지 → pdf.js가 같은 쪽 수로 여는지(`textWorker.js`를 스레드로 띄워 `pages` 하나만 묻는다) → 읽은 뒤 파일이 바뀌지 않았는지(크기·mtime, 바뀌었으면 다음 저장으로 미룬다) → rename. 첫 덧붙임 직전에 `.papertime/papers/<id>/pdf/base.json`(`{digest, length}`, 맥의 `PDFBase`와 같은 바이트)을 한 번 쓴다. 실측(2026-09-26): 픽스처 12종(표·xref 스트림+객체 스트림·간접/공유 `/Annots`·`ligatures.pdf`·RC4-40/128·AES-128·AES-256(객체 스트림, 암호 있음)) 4회 저장 전부 «덧붙임/0/덧붙임/0»; 코퍼스 20편 × 4회도 같고 첫 저장 4.5–5.3 KB, 지우기 383–648 B, 52 MB 책도 59 ms; qpdf·pypdf·MuPDF·pdf.js·PDFKit이 40개 결과에 새 문제 0; 맥이 포터블이 덧붙인 파일을 `appended(+1377)`로 읽고 그 위에 제 저장 네 번(7203/0/597/0 B)을 얹었고, 포터블은 그 파일을 다시 읽어 덧붙였다. 맥의 접기는 포터블이 쓴 주석이 든 파일도 접는다(위 «접기» 항목 — 비교가 사전이 아니라 표시의 뜻을 본다). 검사: `npm test`의 «Marks go into the PDF as an incremental update», `PAPERTIME_CORPUS=<폴더>`(+`PAPERTIME_CORPUS_OUT`)가 코퍼스 프로토콜을 돌려 저장본을 남기고, `PAPERTIME_MAC_APPENDED_PDF=<파일>`이 맥이 덧붙인 파일을 읽어 덧붙인다. 암호화 픽스처는 `pikepdf`로 만든다(qpdf 실행 파일이 없다).
- **본문 검색은 접은 글을 쪽과 함께 들고 memmem으로 훑는다(0.9.9부터).** 한 편의 검색 글은 한 번 접고(대소문자·발음 구별 기호·합자, `PaperText`) 쪽 오프셋과 함께 캐시에 둔다. 질의는 버퍼 하나를 memmem으로 훑고 부분 맵으로 쪽·구절로 돌아간다. 순위는 순수 함수(`SearchRanking`)라 팔레트가 준비된 라이브러리를 키마다 다시 매긴다. 캐시는 크기와 앞 바이트로 믿는다: **커지기만 한 파일(증분 저장)은 그대로 쓰고**, 바뀐 파일은 다시 읽고, 아무도 안 가진 논문의 글은 한 달 뒤 버린다. 실측(Release): 63편 첫 검색 콜드 15.2 s→4.4 s, 디스크 워밍 2.2 s→51 ms, 키당 순위 6.4→1.6 ms; 630편+노트 1,000개 키당 405–490→12–20 ms. 재는 길: `--papertime-search-bench=<질의|질의>`(첫 결과·끝·키당 순위·히치·메모리), `Scripts/search-probe.swift`는 제 캐시 폴더를 쓴다(`PAPERTIME_TEXT_CACHE`). 포터블은 같은 접기를 제 프로세스에서 한다(`main/textIndex`), 검색 결과가 맥과 같은지 `npm test`가 본다. **새 Swift 파일은 `xcodegen generate` 없이는 빌드에 안 들어간다** — «cannot find 'X' in scope»가 나면 먼저 프로젝트를 다시 만든다.
- **뜻으로 찾기의 엔진(`Semantic` 타겟, 0.9.9부터).** all-MiniLM-L6-v2(Apache-2.0)를 fp16 Core ML로 컴파일해 리소스로 둔다(45 MB, 컴파일된 `.mlmodelc`를 커밋한다 — `swift test`는 모델을 컴파일 못 하고, 읽는 사람 기계에서 컴파일하면 첫 실행에 몇 초다). int8은 상위 5개 순서를 못 지켰다(14/15), fp16은 참조와 코사인 ≥ 0.99997·15/15. 토크나이저(`WordPieceTokenizer`)는 **모델을 학습시킨 Rust 라이브러리의 유니코드**를 따른다: 범주 표는 8.0에서 자르고(`Unicode.Scalar.Properties.age`), 그 뒤 범주가 바뀐 코드 포인트 일곱은 목록(`categoryThen`)으로 답하며, CJK 범위는 그 라이브러리의 오타(0x2B920) 그대로다 — 코드 포인트 528,394개와 코퍼스 구절 11,865개가 `BertTokenizerFast`와 id 하나까지 같다(`Scripts/wordpiece-sweep.py` + `Scripts/wordpiece-probe.swift compare`). 청크는 약 100단어·겹침 25, 벡터는 청크 글의 해시로 키를 잡은 fp16(`SemanticVectorStore`, PTSV v1 — 포터블과 파일이 같다), 검색은 전수 비교(13만 개 6 ms). 임베더는 길이를 버킷(16…256, 행 1/16/32/64)으로 묶어 GPU가 모양 몇 개만 한 번 컴파일하게 한다 — 안 묶으면 새 모양마다 130–180 ms. **질의는 CPU 인스턴스**(1.3 ms, 색인 뒤에서 기다리지 않는다, +1 MB), 색인은 GPU(1.4 ms/구절, +84 MB, 끝나면 놓는다). LEANN 같은 그래프 색인은 검토 뒤 뺐다: 질의당 1–6 s, Python 사이드카, iOS 불가. 포터블은 같은 모델을 fp16 ONNX(43 MB)로 onnxruntime-web의 Node 플레이버로 유틸리티 프로세스에서 돌린다(`main/semanticWorker.ts`): 코사인 최저 0.9999997, 상위 5개 15/15, 22 ms/구절·5 ms/질의(WASM 4스레드). onnxruntime-node는 세 배 빠르지만 플랫폼별 바이너리 넷을 아무도 여기서 못 돌려 보니 WASM으로 간다(`loadModel` 하나가 바꿀 자리). esbuild `platform:"node"`가 패키지의 `node` export 조건으로 Node 플레이버를 고르고(깊은 경로는 export 안 됨), 글루 `.mjs`는 디스크의 파일 URL이어야 해서 `out/main/semantic/**`를 asarUnpack한다. ONNX의 fp16 저장은 세션을 만들 때 fp32로 캐스트되어 연산은 fp32다 — Core ML의 −10000 마스크 우회가 여기엔 필요 없다. 포터블 토크나이저는 `age`가 없어 코드 포인트별 표를 그대로 들고 다닌다(표가 곧 그 컷이다). 저장소 파일(PTSV v1)은 두 빌드가 바이트까지 같다(`src/test/fixtures/macSemantic.bin`은 Swift가 쓴 것). 맥 앱에서는 `SemanticIndex`(App/Model, macOS 전용)가 S1의 `PaperTextIndex.text(for:)`에서 글을 받아 PDF를 다시 열지 않고 자른다. 논문마다 매니페스트(도장+자른 자리, `passages/<uuid>.json`)와 앱 하나에 fp16 저장소 하나(`vectors.ptsv`)를 S1 텍스트 캐시 옆에 둔다 — 라이브러리 폴더 안이 아니다(클라우드에 45 MB짜리 변경을 만들지 않는다). 도장이 같으면 아무것도 안 하고, 글이 바뀌면 다시 자르되 새 키만 임베딩하며, 한 달 없던 논문은 쓸어 낸다. 라이브러리가 준비되고 5초 뒤, 팔레트가 열리고 2초 뒤에 돈다. 팔레트의 «뜻이 비슷한 구절» 절은 색인이 다 될 때까지 아예 없고(스피너 없음), 만드는 동안은 3차 색 발판 한 줄 «뜻으로 찾기 준비 중 · n/N»만 있다. 설정 → 읽기의 «뜻으로 찾기» 스위치(`AppSettings.searchByMeaning`)로 끈다. **노트도 색인한다(0.9.10부터).** 논문 다음에 노트를 자른다 — 마크다운 기호만 걷어 내고 낱말은 전부 남긴 글(`NoteText.plain` / `shared/semantic/noteText.ts`, 같은 픽스처를 두 쪽 테스트가 돈다 — 이 규칙이 캐시 키의 일부라 바꾸면 노트 전부를 다시 임베딩한다)을 같은 100/25 창으로 자르고, 벡터는 같은 PTSV 저장소에 든다(포맷은 안 바꿨다). 맥은 매니페스트 `notes/<id>.json`에 글의 SHA(파일 도장 대신)와 자른 자리를 두고, 히트는 `Hit.origin`이 `.paper`/`.note`로 갈리며 사라진 노트의 매니페스트는 바로 지운다(한 달 유예는 논문만); 노트를 고치면 `NotesModel.update`가 `scheduleNotes()`로 5초 뒤 노트만 다시 자른다(`ready`가 된 뒤에만 — 첫 빌드가 노트를 함께 든다). 포터블은 논문의 `summaryNote`를 `note:<id>` 한 쪽짜리 페이지로 보내고(`SemanticPage.note`), 서명에 노트 해시가 들어 `paper:state`의 `summaryNote` 패치가 5초 뒤 다시 짓는다. 줄은 «<노트 이름> · 노트»에 note 아이콘이고 누르면 노트가 열린다 — 맥은 `NotesModel.reveal`에 구절의 낱말을 실어 `NoteEditor`가 앞 낱말 몇 개(8→3)를 찾아 스크롤·깜빡임(색인의 오프셋은 평문 기준이라 마크다운 편집기에서는 못 쓴다), 포터블은 그 논문의 노트 탭. 논문 셋·노트 둘까지 한 출처에서. 실측(Release, 10편+노트 5): 노트 5구절 31 ms, 다시 열면 19 ms, «kimchi stew recipe»에 그 문장을 가진 노트가 0.799로 1위(포터블도 같은 값). 창 없이: `--papertime-semantic-index=1`이 «5 notes in 5 passages»와 «(n from notes)»를 찍고, `--papertime-open-note=<id> --papertime-note-reveal=<낱말>`이 편집기가 어디로 갔는지 찍는다; 포터블 `window.__papertimeMeaning.search`의 히트에 `note`가 실린다. 실측(Release, 10편): 1,532 구절 3.2 s(2.12 ms/구절), 다시 열면 136 ms, 질의 3.4 ms(첫 질의는 모델 로드 208 ms), 메인 스레드 히치 없음. 창 없이 확인: `--papertime-semantic-index=1`, `--papertime-semantic-query=<말>`. `--papertime-window-dump=1`은 접근성 프록시에 KVC를 던져 팔레트가 열려 있으면 죽었다 — `responds(to:)`로 가린다. 포터블의 팔레트도 같은 절과 같은 스위치를 갖는다(`main/semantic.ts`의 `SemanticSearch`, 렌더러 `meaningSearch.ts`; 저장소는 `userData/Semantic/vectors.ptsv`, 프로브 실행은 `$TMPDIR/Paper Time probe/Semantic`). 글은 텍스트 서비스의 `texts` 요청으로 받아 PDF를 두 번 읽지 않는다. **채우는 동안 «준비됐다»고 하지 않는다** — 첫 프로브가 채우는 중에 검색해 라이브러리의 3분의 1에서 답을 받았고, 앱이 끝나면서 대기 중인 약속이 «build failed»로 거절됐다; `ready`는 모든 구절에 벡터가 있다는 뜻이고 그 사이는 발판 줄이 맡는다. WASM은 이 맥에서 56 ms/구절(맥 GPU는 4.8)이라 60편이면 한 번에 10분쯤의 배경 CPU다 — 유틸리티 프로세스라 창은 안 멈추지만(11,794 프레임에 34 ms 넘는 틈 0), 그래서 저장소와 매니페스트가 남아야 하고 채우기는 취소할 수 있어야 한다. **의존성을 더한 가지를 합치면 본 체크아웃에서 `npm install`** — 워크트리의 `node_modules`는 본 체크아웃으로 가는 링크라 거기서만 깔린 것처럼 보인다.
- **Latex Suite(0.9.9부터).** 정답은 스펙이 아니라 **플러그인이 낸 답**이다: Obsidian Latex Suite 1.13.1을 jsdom에서 그대로 돌려 받은 픽스처(`Tests/PaperCoreTests/Fixtures/latex-suite-*.json`, 290건 + 무작위 500건 + 캐럿마다의 문맥)를 맥 `PaperCore/LatexSuite`와 포터블 `shared/latexSuite/`가 **둘 다** 돈다(파일 하나하나가 짝이고, 스니펫 JSON은 맥의 `Resources/LatexSuiteSnippets.json` 하나를 esbuild가 렌더러에 넣는다). 무작위 키 1만 3천 건을 맞대 보고 나서야 보인 것: 캐럿 여럿이 한 트리거에 겹치면 CodeMirror `ChangeSet.of`의 매핑을 그대로 따라야 하고, `\end{…}` 이름을 고치는 동안 lezer의 오류 복구가 환경을 `\end{` 바로 뒤에서 끝낸다. 일부러 다른 것 하나: 인라인 `$$x$$`의 `$$` 앞에서 `/`를 치면 플러그인은 RangeError로 키를 흘리고, 엔진은 그냥 보낸다. ICU는 서로게이트 쌍을 한 글자로, JS는 두 단위로 읽으므로 엔진은 ICU에 U+FFFD를 보여 주고 캡처는 원문에서 자른다(픽스처는 오프셋으로 적는다 — 반쪽 서로게이트는 `JSONDecoder`가 파일째 거절한다). **키 한 번마다 도는 코드는 Debug에서도 잰다**: 2만 자 문단에서 `for … in` 범위와 `Array(slice)`가 3 ms, `while`+버퍼 포인터가 0.07 ms(`LS.bulkUnits`); 포터블도 줄 끝을 한 글자씩 걷던 것을 `indexOf`로 바꿨다. 시간 재는 Swift Testing 시험은 `.serialized` 부모 밑에 둔다 — 그래도 `xcodebuild`가 옆에서 돌면 p95가 한계를 넘길 수 있으니, 그때는 단독으로 다시 돌린다. 맥 어댑터는 `LatexSuiteTextView` 하나를 노트와 글 카드가 같이 쓴다. 제 undo 관리자를 가진 뷰는 `undo:`/`redo:`와 메뉴 검증을 직접 구현한다 — Edit 메뉴의 ⌘Z는 응답 체인에서 `undo:`에 먼저 답하는 쪽으로 간다. 포터블 textarea에 글을 넣는 길은 `execCommand('insertText')` 하나다(`value =`는 되돌리기 스택을 날린다; `beforeinput`을 먼저 보내 아무도 안 막았으면 넣는다). textarea의 되돌리기는 되살린 글자를 **골라 놓으므로** 어댑터가 단계마다 캐럿 자리를 적어 두었다가 `historyUndo`에서 되돌린다. 창 없이 확인: 맥 `--papertime-latex-typing=<스크립트>`, 포터블 프로브의 `type`/`key`. **글자를 치는 프로브는 `insertText`로 넣고 이름 있는 키만 `keyDown`으로** — 글자까지 `interpretKeyEvents`로 보내면 사용자가 켜 둔 두벌식을 지나 «a»가 «ㅁ»이 된다. 호출로 친 키는 undo 그룹을 안 닫으므로 키마다 `NSApp.postEvent`로 `applicationDefined` 이벤트를 제 큐에 넣는다. 숨긴 프로브는 1~2분 뒤 App Nap에 잠든다 — 시간을 재면 `ProcessInfo.beginActivity(.userInitiated)`를 쥔다. 실제 키보드와 입력기는 사람이 확인한다.

- **노트의 수식은 줄을 건너도 조판되고, 치는 동안 카드가 따라온다(0.9.9부터).** `$$`가 한 줄에서 열리고 다른 줄에서 닫히는 블록(Latex Suite의 `dm`이 만드는 모양)은 `NoteMath.blocks(in:)`(PaperCore, 순수 함수; 포팅 `shared/noteMath.ts`, 같은 12 케이스)가 찾고, `NoteMarkdown`이 그 줄들을 하나로 이어 기존 `$$` 패턴에 태운다 — 인라인 `$…$`는 여전히 한 줄이다. 캐럿이 수식 안에 있는 동안(`NoteMath.span(at:in:)`) `MathPreviewCard`(활성화되지 않는 자식 `NSPanel`, 마우스 무시, 유리 표면 + `Corner.popover`)가 그 스팬의 마지막 줄 아래, 스팬의 시작 x에 뜬다 — 자리가 없으면 위. 80 ms 디바운스, 불완전한 LaTeX는 마지막 성공작을 흐리게(0.45) 두거나 날 글을 2차 색으로 보여 주지 오류 상자는 없다. 선택·raw 모드·포커스 잃음·스팬 밖이면 사라진다. 포터블은 textarea 위에 놓인 `div`(`ui/mathPreview.ts`, 거울 div로 캐럿 자리를 잰다). 창 없이 확인: `--papertime-math-preview=<컨테이너 안 png>`가 `$…$` 안에 `x1`을 치고 `$$` 블록에 `2`를 쳐서 카드의 프레임과 얼굴을 stderr에 찍고 편집기+카드 합성 PNG를 남긴다. 카드의 재질은 `GlassSlab`(App/Views)이다 — **시스템의 `NSGlassEffectView`가 아니다**: 그 뷰는 창의 크롬에 맞춰져 있어 흰 종이 위에서는 테두리만 있는 흰 카드로 보였고, 이 SDK의 SwiftUI에는 `glassEffect`가 없다. 유리를 유리로 읽게 하는 것은 블러가 아니라 **렌징**이다(애플의 «Meet Liquid Glass» 세션: «이전 재질은 빛을 흩뜨렸고 이 재질은 빛을 굽히고 모은다»). 그래서 슬랩은 손으로 그린 띠가 아니라 **광학체로 계산한다** — 애플 참조에 맞춰 보정된 재현(`Oliverrr2424/webgl-apple-liquid-glass`)의 모델을 픽셀마다 그대로 돈다: 둥근 사각형 SDF 위에 **두께 필드**(가운데는 평평, 테두리 `bevel`은 4분원 단면으로 0까지 — 작은 컨트롤의 규칙대로 짧은 변의 30%, 5–18pt, 높이는 21×bevel/34), 그 단면의 **법선은 테두리에서 안쪽으로** 기운다(액체의 메니스커스 — 볼록 렌즈면 안쪽이 확대되지만 애플의 것은 **바깥 것이 테두리로 눌려 들어온다**), 그 면을 지나는 시선을 **스넬 법칙**으로 굴절시켜(IOR 2.0, R·B는 ±0.14로 갈라 색수차, 경로 길이 = 높이×(0.25…1)×4.5, tanh로 1.15×bevel에 묶음) 바닥에 닿는 자리의 바탕을 표본한다 — 그래서 카드 위 4pt에 있는 글자 줄이 위 테두리로 끌려 들어오고, 아래 테두리는 카드가 드리운 그림자를 비춘다(뷰의 그림자 둘을 `GlassSlab.Shadow`가 같은 값으로 렌더러에도 준다; 카드 밑에는 그림자가 없다 — 카드가 덮으니까). **산란은 두께를 따른다**: 테두리 1.5pt·중간 3.5pt·판 7pt 세 단계의 블러를 t로 섞는다(둘만 섞으면 선명한 테두리와 흐린 몸체 사이에 «고리»가 선다), 투과광에만 채도 1.35. **프레넬**(Schlick, F0 4%)이 스치는 테두리에서 주변 — 바탕(14pt 블러)과 왼쪽 위 램프가 있는 방(램프 쪽 1.0, 반대쪽 0.55) — 을 비추고, 두 스펙큘러 로브(램프 쪽 지수 10, 반대쪽 0.5배)가 베벨 띠(t 0.015–0.87)에 앉으며, **밝은 선(0.30)은 실루엣 자체에**(중심 0.7pt 안, ±1.1pt) 놓고 그 바깥에는 아무것도 어둡지 않다(라이트의 방은 사방 1.0, 윤곽 0). 몸체는 **흡수**(두께비례, 라이트 6.5/5.5/4%, 다크는 −6% 들어올림)와 «regular»의 적응 틴트(라이트 0.74/0.76/0.79, 다크 0.05/0.055/0.065, 0.35 — 245 종이 위에서 몸체가 약 235로 **베벨 띠와 같은 톤**이 되는 값), 그리고 램프 쪽이 밝은 몸체 안 산란(±3.5%). **층은 둘뿐이어야 한다 — 실루엣의 흰 링, 그리고 균일한 몸체.** 사용자가 확대해 센 것은 넷이었다: 실루엣의 어두운 선(6% 윤곽 + 어두운 방 반사) → 흰 링 → 굴절된 그림자가 만드는 회색 «해자» → 그보다 밝은 판(«하얀 배경»). 어두운 선은 지우고, 링을 실루엣으로 옮기고, 그림자를 옅게(접촉 0.10, 부드러운 0.12·반경 10·낙차 7 — 위로 번지지 않게) 하고, 판을 해자와 같은 회색으로 내려 없앴다. 측정은 스크린샷의 한 열·한 행을 픽셀 값으로 찍어서 한다(스크래치 `px`/`pxh`) — 눈으로는 «흰 카드»라고만 읽힌다. 계산은 선형 광에서 하고 sRGB로 인코드한다. **흰 종이 위의 유리에는 굽힐 것이 없다** — 거기서 남는 단서는 위 줄의 글자, 굴절된 그림자, 링, 흡수 회색, 산란 그라디언트뿐이니 그 다섯을 지운 «순수 물리»로 되돌리지 마라. **패널은 카드보다 `MathPreviewCard.inset`(32pt)만큼 크다** — 카드 크기의 패널은 슬랩의 그림자를 사각형으로 잘랐고, 그 사각형의 각진 모서리가 «모서리 없는 맨 바깥 테두리»로 보였다(`frame`은 카드, `pictureFrame`은 패널). 슬랩은 카드 아래·둘레(`GlassSlab.margin` 24pt)의 그림을 받고, 렌더(400×46pt 40–60 ms)는 `MathPreviewCard`가 **메인 스레드 밖**에서 돌려 세대 번호로 늦은 것을 버린다(그동안 같은 크기의 지난 슬랩을 깔아 둔다); 보고의 `slab N ms`가 그 시간이다. **바탕은 창 서버에서 가져온다**(`CGWindowListCreateImage`, 이 창 하나만 — 다른 앱 창이 아니라서 화면 녹화 권한이 필요 없다): 노트 판, 그 아래의 바탕, 글자가 화면 그대로 들어온다. 화면 밖 창(프로브)에는 픽셀이 없어 텍스트 뷰의 `cacheDisplay`로 떨어지는데, **배경을 안 그리는 텍스트 뷰의 스냅샷은 투명이고 투명을 흩뜨리면 회색이 된다** — 창의 종이색을 먼저 칠한다. 패널의 `hasShadow`는 끈다. 재질만 볼 때는 스크래치의 `ImageRenderer` 하네스(흰 노트+바로 위 글자 줄·회색 위 초록 토글·앨범아트·다크 네 장면)로 그려 **2배 픽셀 크롭으로** 본다 — 시트를 축소해 보면 1–2px의 링·선·그림자가 다 사라져 «안 바뀌었다»로 읽힌다(실제로 그렇게 두 번 헛돌았다). 앱 안에서는 `--papertime-math-preview-onscreen=1`이 프로브 창을 주 화면 오른쪽 아래에 몇 초 띄워 창 서버의 바탕으로 찍는다(사람이 허락했을 때만). 프로브 보고의 `ground window|view`가 어느 길이었는지 말한다. **프로브의 창은 `WindowProbe.ownWindow`로 만든다** — `constrainFrameRect`를 무시하는 하위 클래스라 AppKit이 화면으로 못 끌어온다. 평범한 `NSWindow`를 −60000에 두고 띄웠더니 두 번 중 한 번(다크 실행) 주 화면 (290, 282)에 떠서 프로브 내내 거기 있었다; 띄운 뒤 `WindowProbe.isOnAScreen`으로 다시 보고, 화면에 있으면 즉시 내리고 아무것도 치지 않는다. **자식 창은 부모가 쥐고 있어 주인보다 오래 산다** — 카드가 노트를 닫은 뒤 노트 목록 위에 남았다; `deinit`과 `viewWillMove(toWindow: nil)`·`viewDidHide`에서 패널을 내린다. **동적 색을 그리기 패스 밖에서 비트맵에 그리면 라이트 값으로 풀린다** — 다크 노트의 수식이 검정이었다; `NoteMarkdown.render(appearance:)`가 텍스트 뷰의 `effectiveAppearance` 아래서 `labelColor`를 풀어 넣고 수식 캐시 키에 appearance 이름을 넣는다(프로브가 첨부 이미지의 잉크 밝기를 찍는다: 라이트 0.00, 다크 1.00). **자식 `NSPanel`은 편집기의 `cacheDisplay`에 안 찍힌다** — 떠 있는 카드를 사진에 넣으려면 패널의 비트맵을 따로 그려 변환한 프레임에 합성한다(`NSTextView`는 뒤집힌 좌표, `NSImage`는 아니다). 포터블 프로브의 `type` 단계는 `{selector, text, at}`이고 노트 textarea는 `.note-area`다(제목 칸도 `.field textarea`라 그걸로 잡으면 엉뚱한 데 친다).
- **검색 결과 목록(«찾은 것»)에도 «뜻이 비슷한 구절» 그룹이 있다.** 팔레트에서 Enter를 치면 가운데 칸이 `searchResults` 범위가 되고, «제목에서»·«논문 안에서» 뒤에 `PaperTable.Entry.meaning` 줄이 선다(`SemanticIndex.shared.hits(k: 8)`, 낱말로 이미 찾은 구절은 뺀다; 팔레트가 닫힌 뒤에도 목록은 남으므로 색인이 준비될 때까지 500 ms마다 다시 묻되 2분에서 그만둔다). 포터블은 `store.searchMeanings` + `askListMeaning`. **`NSHostingView`보다 좁은 SwiftUI 뷰는 가운데에 놓인다** — 구절 줄이 폭 없는 `Button`이라, 스니펫과 부제가 둘 다 짧은 딱 한 줄(`<latexit` 앞에서 잘린 것)만 100 px쯤 오른쪽에 떠 있었다. `ImageRenderer`로 재현(짧은 줄 첫 잉크 x=81, 긴 줄 11)했고, `.frame(maxWidth: .infinity, alignment: .leading)`이 답이다; `PaperTable.body(for:)`는 제목 줄 말고 모든 줄을 앞 가장자리에 붙인다. 확인: `--papertime-search-results=<말>` + `--papertime-window-shot-after=25`.

## 말투
앱과 배포 페이지의 **한국어는 토스의 말투(해요체), 영어는 애플의 말투**다. 정본은 `Docs/Voice.md`이고, 문자열을 새로 쓰거나 고칠 때마다 그 문서의 마지막 절(고칠 때 확인하는 것)을 지나야 한다. 요약하면: 한국어는 해요체로 쓰고 피동형과 한자어를 피하며 한 문장에 한 가지만 담는다(단추는 "보내기"처럼 명사형, "보내요"가 아니다). 영어는 능동태 현재형 짧은 평서문이고 *powerful·seamless·simply·easy* 같은 말을 쓰지 않으며 메뉴와 단추는 Title Case, 문장은 sentence case다. **둘은 번역 관계가 아니다** — 한 쪽을 직역하면 둘 다 어색해진다. 두 언어는 `L(ko, en)`(맥)과 `L(ko, en)`(`Portable/src/shared/lang.ts`)로 소스에 나란히 두고, 배포 페이지는 `data-l="ko"`/`data-l="en"` 쌍으로 마크업에 나란히 둔다 — 어느 쪽도 카탈로그로 빼지 않는 이유는 같다. 떨어뜨려 두면 한 쪽이 조용히 썩는다. 어느 말로 보일지는 시스템 언어가 정하고(한국어면 한국어, 그 밖이면 영어), 사용자가 뒤집을 수 있다.

## 기능 소개(스니펫) 규칙
새 기능이 **중요한 기능**이면 — 즉 한 문장으로는 무엇인지 와닿지 않거나, 사용자가 모르면 영영 안 쓰게 될 기능이면 — 코드만 쓰고 끝내지 않는다. 다음을 **같은 커밋에서** 함께 한다.

1. `ReleaseNotes.Demo`에 케이스를 하나 더하고, `App/Views/FeatureDemos.swift`에 그 데모를 만든다.
   - 녹화 영상이 아니라 **동작하는 SwiftUI**로 만든다. 영상은 번들을 불리고, UI를 고치는 순간 낡고, 만져볼 수 없다.
   - `DemoScale`을 받아 `.compact`(로그용)와 `.full`(About·환영 화면용) 두 크기로 자란다. 두 벌을 따로 만들지 않는다.
   - 단축키는 `app.shortcut(for:)`로 읽는다. 사용자가 키를 바꾸면 데모도 따라 바뀌어야 한다.
   - 실제 UI와 같은 형태로 만든다 — 같은 카드, 같은 행 높이, 같은 분류 캡션. 그림이 아니라 축소판이어야 한다.
2. `ReleaseNotes.releases`의 해당 버전 `added`에 항목을 더하고 `demo:`를 건다. 제목은 키워드, 설명은 한 문장.
3. 헤드라인급이면 `ReleaseNotes.highlights`에도 더한다. 그러면 **About과 환영 화면 양쪽에 자동으로** 실물 크기로 나온다(`FeatureShowcase` 하나를 둘이 같이 쓴다).
4. 필요하면 `ReleaseNotes.groups`(모든 기능 목록)에도 한 줄 더한다.
5. 한글을 먼저 쓴다. `Text2(ko, en)` / `ReleaseNotes.string(ko, en)`.
6. 다른 앱과 다른 점이 핵심이면 데모 안에서 **나란히 비교**한다(주석의 "다른 PDF 앱 / Paper Time", 책의 자르기 전·후 토글처럼). 한 문장으로 "더 예쁘다"보다 두 그림이 낫다.
7. 창을 띄우지 않고 확인한다: `PAPERTIME_RENDER_DEMOS=<쓸 수 있는 폴더>`(샌드박스 안, 예: `~/Library/Containers/com.imtaeheon.PaperTime/Data/tmp/demos`)로 앱을 실행하면 모든 데모를 두 크기로 PNG로 그리고 끝난다. `ImageRenderer`라 ScrollView 내용과 ProgressView는 비어 보이는데, 그건 렌더러의 한계다.

## 브랜치 규칙
버전마다 가지를 셋으로 나눈다. `v0.1.0`과 `v0.1.0/dev`는 git이 동시에 가질 수 없는 이름이라(ref 충돌) 접두사를 앞에 둔다.

| 가지 | 무엇을 하는 곳 |
|---|---|
| `main` | 닫힌 버전만 들어온다. 직접 작업하지 않는다. |
| `v0.1.0` | 이번 버전. `main`에서 갈라져 나온다. |
| `dev/v0.1.0` | **새 기능**은 여기서. |
| `debug/v0.1.0` | **오류 수정**은 여기서. |
| `ipad/v0.1.0`, `iphone/v0.1.0` | 기기별 이식은 버전 밑에 기기 가지로. |

플랫폼은 그 위에 따로 셋이다 — `mac`(스위프트 앱), `windows`, `linux`. 뒤의 둘은 같은 커밋에서 갈라져 나왔고 공유 파일(`Portable/`)이 동일하므로, 서로 합치는 일은 바뀐 것만 따라가는 fast-forward다. 자세한 것은 `Docs/Porting.md`.

흐름:

```bash
# 버전을 연다
git switch main && git switch -c v0.1.0
git branch dev/v0.1.0 v0.1.0 && git branch debug/v0.1.0 v0.1.0

# 개발 중 — 기능은 dev, 수정은 debug
git switch dev/v0.1.0     # 새 기능
git switch debug/v0.1.0   # 오류 수정

# 안정화되면 둘 다 버전 가지로 합치고, 버전을 닫는다
git switch v0.1.0
git merge --no-ff dev/v0.1.0 debug/v0.1.0
git switch main && git merge --no-ff v0.1.0 && git tag 0.1.0   # 태그는 v 없이 — 브랜치 v0.1.0과 이름이 겹친다
Scripts/publish-release.sh 0.1.0  # DMG를 만들어 릴리스에 올리고 배포 페이지를 다시 쓴다
git push origin main --tags
```

**고친 것을 바로 배포하지 않는다.** 먼저 `Scripts/build-installers.sh`로 작업 중인 코드 그대로 구워서 `Installers/`에 두고, **사용자가 직접 설치해서 확인한 뒤에** `Scripts/publish-release.sh`로 올린다. 순서를 지키는 이유는 0.7.1에서 겪었다 — 창 뒤에 깔려던 바탕이 앱을 통째로 덮었는데, 디버그 빌드에서는 우연히 뒤에 남아 있어서 배포 빌드에서만 드러났다. 확인은 **Release 빌드**로, 그것도 **설치된 사본**으로 해야 한다. `Installers/`는 git에 들어가지 않고(수백 MB), 남는 사본은 릴리스 쪽이다. 그래서 배포가 끝나면 `publish-release.sh`가 `Installers/`와 `Portable/dist/`에서 방금 올린 버전이 아닌 것을 지운다 — 올리기 전이 아니라 올린 뒤에 지우니, 업로드가 실패하면 아무것도 잃지 않는다. 배포 스크립트는 electron-builder가 돌고 있으면 시작을 거부한다 — 다 써지지 않은 패키지를 올리면 GitHub이 릴리스 전체를 되돌린다.

배포 페이지는 `Website/`에 있고 `gh-pages` 가지에서 서비스된다 —
<https://icecoffee2500.github.io/paper-time/>. `Scripts/publish-release.sh <tag>`가
`Scripts/make-dmg.sh`를 불러 DMG를 만들고, GitHub 릴리스에 올리고,
`Website/releases.json`을 다시 써서 `gh-pages`로 옮긴다. 그 json은 소스 쪽에도
같이 커밋한다. `Docs/`는 이 프로젝트가 스스로 두는 메모이지 배포 페이지가 아니다
(대소문자를 가리지 않는 디스크에서 `docs/`로 보여도 git은 가린다).

배포 페이지의 **다운로드 수**는 GitHub가 릴리스 자산(DMG)마다 세는 `download_count`다 — 첫 DMG를 올린 날부터 세고 있어서 페이지에 보이기 전의 것도 들어 있다. `publish-release.sh`가 그때의 값을 `releases.json`의 `downloads`에 박아 두고(API가 막혔을 때의 대비), 페이지(`demos.js`의 `fetchDownloadCounts`)는 열릴 때 `api.github.com/repos/…/releases`를 직접 불러 산 값으로 바꾼다(인증 없이 IP당 시간에 60번 — 랜딩 페이지에는 넉넉하다). **그 숫자에는 확인하느라 내려받은 것도 다 들어간다** — 배포 확인은 릴리스 자산을 `curl`로 받지 말고 `dist/`에 만들어진 같은 DMG로 하라(`gh release view <tag> --json assets`의 `digest`로 같은 파일인지 대조할 수 있다). 2026-09-17 기준 10회 중 7회가 그렇게 내가 받은 것이었다.

배포 페이지는 두 쪽이다 — `index.html`은 **고를지 말지 정하는 사람**이 읽고, `docs.html`(설명서)은 **질문을 갖고 온 사람**이 읽는다. 그래서 앞쪽은 주장하고 뒤쪽은 답한다: 같은 토큰·같은 반경·같은 강조색을 쓰되 활자는 작고 단수는 좁으며, 데모가 띠의 주인공이 아니라 문장 사이에 앉는다. 두 쪽은 **막대의 파란 캡슐로 서로를 가리킨다** — 앞쪽의 캡슐은 «설명서», 설명서의 캡슐은 «받기». 캡슐 옆의 이름들(읽기·표시·노트…)은 전부 **이 쪽 안에서 움직이는** 것이라서, 쪽을 떠나는 «설명서»가 그 줄에 같이 서 있으면 헷갈린다. 앞쪽 막대에 «받기»를 또 두지 않는 이유도 같다 — 바로 아래 히어로에 있고, 막대의 것은 그 자리로 스크롤만 했다. 설명서의 데모는 **앞쪽의 그 데모 그대로**다(`demos.js`가 `window.PaperTime`으로 내주고 `docs.js`는 어느 슬롯에 무엇을 놓을지만 말한다) — 데모를 두 번 쓰면 한 번은 반드시 낡는다. 왼쪽 레일은 읽고 있는 절을 가리키고(가장 위에 있으면서 아직 바를 지난 절; 마지막 절은 페이지 끝에 닿으면 켠다), 좁은 화면에서는 눕는다. `demos.js`는 `document.body.dataset.page !== "docs"`일 때만 앞쪽을 스스로 띄운다.

페이지만 바꾼 것(문장, 카드, 스크립트)은 릴리스 없이 `Scripts/publish-page.sh "메시지"`로 gh-pages에 올린다 — `publish-release.sh`도 마지막에 이걸 부른다. 그런 커밋은 `releases.json`과 마찬가지로 `main`에 직접 둔다(앱 버전이 아니라 페이지의 일이다). 링크를 붙였을 때 메신저가 펼치는 카드(`og:image`)와 파비콘은 `Scripts/page-images.swift`가 앱 아이콘에서 그린다 — `swiftc -O Scripts/page-images.swift -o /tmp/page-images && /tmp/page-images`. 카드는 JPEG다(바탕이 그라디언트라 PNG는 1.5 MB, JPEG는 170 KB). `og:image`가 없으면 카카오톡 같은 곳은 페이지의 첫 `<img>`를 집어 간다 — 그게 설치 안내의 보안 대화상자 스크린샷이었다.

한쪽에서 고친 것이 다른 쪽에 당장 필요하면, 그 가지를 기다리지 말고 `git merge debug/v0.1.0`으로 끌어온다. 버전을 닫기 전까지 `main`은 건드리지 않는다.

개발 현황은 `python3 Scripts/board.py`로 본다 — 가지별 상태와 커밋 그래프를 HTML로 그려서 열어준다.
